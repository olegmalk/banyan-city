# Canary 3 on the 5090. The first clip rendered fine and was garbage: abstract
# smears unrelated to the conditioning image, which means one of the 16GB
# workarounds (pre-computed embeddings / force-named pipeline class) was
# bypassing the image path. A 24GB card needs none of them, so this runs in one
# process with the pipeline class the model's own config names, and the pipeline
# does its own encoding. It also prints that class name, so we learn what the
# repo actually is.
tasks:
- id: vid-5090-canary3-1785505432
  worker: rtx5090
  video: true
  node: 001-capability-inventory
  beats: '10'
  motion: the underground root map pulses slowly, veins of water drifting through
    the soil, rings of light spreading outward, slow push-in
  size: 704x1280
  seconds: 3.4
  steps: 30
  seed_base: 20260731
