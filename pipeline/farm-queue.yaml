# MOTION TAKES — the open requests want takes, not frames (founder).
# The video stack installs itself on first run (video_task.ensure_stack)
# with a courier mark at every stage. Canary first: one clip proves the
# path before any batch.
tasks:
- id: vid-canary-1785437811
  worker: msi
  video: true
  node: 001-capability-inventory
  beats: '10'
  motion: the underground root map pulses slowly, veins of water drifting, rings of
    light spreading outward through the soil, slow push-in
  size: 704x1280
  seconds: 4
  steps: 30
  seed_base: 20260731
