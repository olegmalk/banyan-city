# 2026-08-03 09:0xZ — "needs more movement, hands barely move". Two parameters.
#
# Founder on the A/B: "B is better overall, but its still pretty lifeless, needs
# more movement, hands barely move." B (shake terms off) is now the default for
# motion beats — decided per beat in video_prompt, so beats 4/6/8 that are meant
# to be still keep the suppression. That is landed, not queued.
#
# What is NOT solved is the lifelessness, and I do not know the cause yet. Two
# candidates, both single parameters, tested one variable at a time:
#
# FRAMES. Wan 2.2 TI2V-5B is native 121 frames at 24fps (5s) at 704x1280. We have
# been asking for 61 (2.5s) — half its trained length. A model asked for half its
# native sequence may simply distribute less motion. render_t3 already caps long
# footage to the beat's scripted length, so a 5s render is usable for a 2.5s beat:
# we get the model's native motion and trim the tail. Costs ~2x per clip.
#
# GUIDANCE. 5.0 anchors hard to the prompt AND to the init frame. Lower guidance
# usually loosens i2v toward more movement; too low drifts off the composition,
# which is the failure the founder already rejected once (the extra scene). 3.0 is
# a step, not a leap.
#
# A tests frames alone, B guidance alone, C both — so if C is best we still know
# which half did the work.
tasks:
- worker: rtx5090
  video: true
  node: 001-capability-inventory
  video_model: ti2v-5b
  motion: no new subjects, no scene change
  size: 704x1280
  offload: true
  seconds: 5.0
  steps: 14
  guidance: 5.0
  seed_base: 20260805
  id: life-A-native5s-b01-1785796800
  beats: 1
- worker: rtx5090
  video: true
  node: 001-capability-inventory
  video_model: ti2v-5b
  motion: no new subjects, no scene change
  size: 704x1280
  offload: true
  seconds: 2.5
  steps: 14
  guidance: 3.0
  seed_base: 20260805
  id: life-B-cfg3-b01-1785796800
  beats: 1
- worker: rtx5090
  video: true
  node: 001-capability-inventory
  video_model: ti2v-5b
  motion: no new subjects, no scene change
  size: 704x1280
  offload: true
  seconds: 5.0
  steps: 14
  guidance: 3.0
  seed_base: 20260805
  id: life-C-both-b01-1785796800
  beats: 1
