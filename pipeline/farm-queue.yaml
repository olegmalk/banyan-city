# 2026-08-03 09:4xZ — "motion blur" was in our NEGATIVE prompt the whole time.
#
# Three hypotheses tested and dead: frames (0.62 -> 0.59, worse), the static scene
# anchor (0.62 -> 0.54, worse), and guidance — which turned out to be an INVALID
# test, because --guidance never reached the single-beat renderer at all (the cfg
# 3.0 clip was byte-identical to cfg 5.0, same sha256). Fixed; guidance is now
# untested rather than ruled out.
#
# The one change that has ever moved the needle was removing OUR OWN shake terms
# from the negative: 0.19 -> 0.62, 3.3x. That pointed at the negative prompt, and
# reading it properly found two more motion suppressors sitting in the STYLE list:
#
#   "motion blur"  fast hands PRODUCE motion blur. We asked for fast typing while
#                  forbidding its visual signature. The cheapest way to satisfy
#                  both is not to move. Added 2026-08-01 to fight photorealism,
#                  when the complaint was style and nobody was thinking about
#                  motion.
#   "film still"   meant as "not a live-action film frame", but the token *still*
#                  is in it. Same trap as WANTS_STILL matching "hands going still"
#                  earlier today.
#
# Both now apply only to beats whose direction asks for stillness, where they cost
# nothing. G measures beat 1 with them gone. Nothing else changed from the current
# default, so the delta is theirs alone.
tasks:
- worker: rtx5090
  video: true
  node: 001-capability-inventory
  video_model: ti2v-5b
  motion: no new subjects, no scene change
  size: 704x1280
  offload: true
  seconds: 2.5
  steps: 14
  guidance: 5.0
  seed_base: 20260805
  id: life-G-noblur-b01-1785799200
  beats: 1
