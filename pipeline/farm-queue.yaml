# 2026-08-03 08:2xZ — RE-RENDER THE TWO BEATS MY OWN WORDING FROZE.
#
# Founder on the model comparison: "wan 2.2 basically doesnt move at all,
# literally." Correct, and not the model's fault — beat 1's direction ends "hands
# going still", WANTS_STILL matched the bare word, and the anti-static terms were
# suppressed on a beat whose whole job is typing. Beat 12 the same. Fixed in
# antistatic_for (first signal wins); these two need re-rendering to benefit.
#
# 14 steps, not 20: the step sweep on the CORRECT path gives 186s vs 239s for what
# looks identical to me at the face crop. Pending the founder's call on the sweep,
# so these two are also a second opinion on 14 steps at the same time.
#
# AnimeGen is NOT requeued. Three attempts, three walls: Ctrl+C, then the stall
# watchdog, then a CUDA OOM in the VAE (fixed), and now exit 3221225477
# (0xC0000005 ACCESS_VIOLATION) = host RAM. load_animegen loads BOTH A14B
# transformers in bf16 before casting to fp8, so the peak is ~64 GiB of host RAM
# and the steady state ~38 GiB even after casting, because cpu-offload keeps
# weights in host RAM. That is not fixable with flags on this machine. The "69GB
# load peak" figure recorded in vet_model.py and dismissed as read off the wrong
# document was right about the number. See MORNING notes.
tasks:
- worker: rtx5090
  video: true
  node: 001-capability-inventory
  video_model: ti2v-5b
  motion: no new subjects, no scene change
  size: 704x1280
  offload: true
  seconds: 2.5
  steps: 14
  guidance: 5.0
  seed_base: 20260805
  id: unfreeze-b01-1785792000
  beats: 1
- worker: rtx5090
  video: true
  node: 001-capability-inventory
  video_model: ti2v-5b
  motion: no new subjects, no scene change
  size: 704x1280
  offload: true
  seconds: 2.5
  steps: 14
  guidance: 5.0
  seed_base: 20260805
  id: unfreeze-b12-1785792000
  beats: 12
