# 2026-08-03 08:4xZ — WHY DOESN'T BEAT 1 MOVE? Two variables, isolated.
#
# Founder: "wan 2.2 basically doesnt move at all, literally", then "we dont need
# him to go still in beat 1 anyway."
#
# The second is done: beat 1's direction is now nothing but typing (the stop
# belongs to beat 2, and the keyboard sfx already ends on its own).
#
# The first is NOT yet explained, and I have to be straight about that. I found a
# real bug — my "hands going still" wording made WANTS_STILL suppress the
# anti-static terms — fixed it, re-rendered, and the measured motion moved 1%:
# 2.68 -> 2.64. So anti-static was NOT the lever. A real bug, fixed, that was not
# the cause.
#
# The remaining suspect is the other thing we changed that day: OUR OWN
# shake-suppression negative, added 2026-08-02 to stop the shaking the founder
# reported. Eight terms including "vibrating", "trembling camera", "unstable
# camera". Those may be damping the hand motion we WANT along with the camera
# motion we do not.
#
# So: same beat, same seed, same steps, same new typing-only direction. The ONLY
# difference is whether SHAKE_NEG is sent. If B moves and A does not, our shake
# fix is the cause and the trade is explicit (a little shake for real motion) —
# the founder's call, and he has a picture to make it on. If both are still, the
# direction and the negative are both innocent and the next suspect is 61 frames
# at 2.5s being too few for a visible hand action.
tasks:
- worker: rtx5090
  video: true
  node: 001-capability-inventory
  video_model: ti2v-5b
  motion: no new subjects, no scene change
  size: 704x1280
  offload: true
  seconds: 2.5
  steps: 14
  guidance: 5.0
  seed_base: 20260805
  id: shakeneg-A-on-b01-1785793200
  beats: 1
- worker: rtx5090
  video: true
  node: 001-capability-inventory
  video_model: ti2v-5b
  no_shake_neg: true
  motion: no new subjects, no scene change
  size: 704x1280
  offload: true
  seconds: 2.5
  steps: 14
  guidance: 5.0
  seed_base: 20260805
  id: shakeneg-B-off-b01-1785793200
  beats: 1
