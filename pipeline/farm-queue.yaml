# The farm's work queue — workers poll this file on origin/main every minute.
# Entry shape is documented in farm_worker.py. The steward queues and clears
# tasks; results arrive on farm-results-<worker> branches with heartbeats.
tasks:
  - id: msi-first-light
    worker: msi
    node: 001-capability-inventory
    beats: "6"
    seeds: 2
