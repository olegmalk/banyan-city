# 2026-08-13. THE SWEEP. His two shape notes applied to every beat that shows the thing.
#
# "all of these beats look fine except the figs which.. just look like weird green balls" —
# and the archaeology behind it: TWENTY-SIX live prompts mention a fig and exactly ONE
# carried shape language, the one where I proved a day earlier that "round green fig" makes
# an apple. I never propagated it. This is the propagation.
#
# TWO CLAUSES, both his: the FIG becomes a teardrop with a narrow neck and matte skin,
# explicitly not a sphere and not an apple; the LEAVES become broad rounded cotyledons rather
# than the pointed lances every plate has drawn. Each beat gets whichever of the two it
# actually shows. Negative gains the failures by name.
#
# QUEUED BEHIND THE SAMPLE ON PURPOSE. ep2-b18-figshape and ep2-b19-figshape are testing the
# same clause and run first. If the sample fails, these are still unclaimed and I pull them —
# authoring ahead costs nothing and keeps the card fed while he is away; rendering ahead of a
# verdict would not.
id: ep2-b03-leafB-0813
task: ep2-b03-leafB-0813
node: 002b-first-citizen
beat: 3
runner: box
priority: 19
needs_gpu: true
max_attempts: 1
sample: true
owner: ep2 motion lane, 2026-08-12
consumer: 'review/ep2-picks/motion-0812.html -- beat 3 re-run with its own object named, on the route two samples proved. Screened as a set.'
success: 'A 704x1280 mp4 of beat 3 with the shape notes applied — the leaves broad and rounded rather than pointed, the beat''s own action unchanged, matching its twin. The failure worth reporting is the shape not moving, which would put it in the plate and not the prompt.'
why: His two shape notes, propagated to every beat that shows the thing rather than left in the one file where I first proved them.
est_minutes: 6
needs:
- cuda
- vram20
- video-venv
env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
payload:
  C:\banyan-farm\ep2-b03-leafB-0813\cover_crop.py: "#!/usr/bin/env python3\n\"\"\"Cover-crop an asserted source image to an exact WxH. No model, no network.\"\"\"\nimport argparse, hashlib, os, sys\nfrom PIL import Image\n\nap = argparse.ArgumentParser()\nap.add_argument(\"--src\", required=True)\nap.add_argument(\"--sha256\", default=\"\", help=\"asserted before anything is written\")\nap.add_argument(\"--out\", required=True)\nap.add_argument(\"--size\", default=\"704x1280\")\na = ap.parse_args()\n\nif not os.path.isfile(a.src):\n    sys.exit(\"!! src not found: %s\" % a.src)\nh = hashlib.sha256()\nwith open(a.src, \"rb\") as fh:\n    for chunk in iter(lambda: fh.read(1 << 20), b\"\"):\n        h.update(chunk)\nhave = h.hexdigest()\nif a.sha256 and have != a.sha256:\n    sys.exit(\"!! SRC SHA MISMATCH -- refusing.\\n   want %s\\n   have %s\" % (a.sha256, have))\n\nW, H = (int(v) for v in a.size.split(\"x\"))\nim = Image.open(a.src).convert(\"RGB\")\nsw, sh = im.size\nscale = max(W / float(sw), H / float(sh))\nnw, nh = int(round(sw * scale)), int(round(sh * scale))\nim = im.resize((nw, nh), Image.LANCZOS)\nleft, top = (nw - W) // 2, (nh - H) // 2\nim = im.crop((left, top, left + W, top + H))\nos.makedirs(os.path.dirname(os.path.abspath(a.out)) or \".\", exist_ok=True)\nim.save(a.out)\nprint(\"src %dx%d sha %s\" % (sw, sh, have), flush=True)\nprint(\"cover-crop scale=%.4f -> %dx%d, centre box (%d,%d)\" % (scale, nw, nh, left, top), flush=True)\nprint(\"WROTE %s\" % a.out, flush=True)"
  C:\banyan-farm\ep2-b03-leafB-0813\b03-motion-prompt.txt: '2D anime, hand-drawn cel animation, static locked framing, the frame never moves, flat cel shading, clean ink linework, anime key art. Subject already in frame: The sapling''s leaves are BROAD AND ROUNDED — wide oval cotyledon leaves with soft round tips, not narrow, not pointed, not lance-shaped and not spiky. a small goblin standing in a field under morning light — pale yellow-green skin, a bald domed head, long pointed ears, red eyes, a patched hooded coat. A SMALL SAPLING STANDS IN FRONT OF HIM — one slender trunk no thicker than his arm with a few leaves at the top, far too narrow to hide anybody. HE DROPS INTO A LOW CROUCH BEHIND THAT SAPLING: he bends his knees deeply and sinks until he is squatting down close to the ground, head lowered to the height of the little tree''s lowest leaves, shoulders pulled in narrow, and from down there he leans his head out to one side to peer past the stem and pulls it back in. He is DOWN LOW, not standing. Both he and the little tree stay fully in frame so the size mismatch reads. He stays in the same place in frame and the camera does not move. Soft morning light, cinematic lighting, detailed, newest, masterpiece, best quality, very aesthetic.'
  C:\banyan-farm\ep2-b03-leafB-0813\b03-negative.txt: pointed leaves, lance-shaped leaves, narrow leaves, spiky leaves, camera pan, camera tilt, panning, tilting, camera movement, dolly, zoom, push in, pull back, tripod, camera, camera equipment, film equipment, stool, stake, plant support, standing upright, standing straight, full height, face morphing, distorted face, warped face, changing facial features, extra eyes, extra ears, hair, walking out of frame, leaving the frame, drifting sideways, moving to a different spot, second character, duplicate character, scene change, shot change, new camera angle, different location, photorealistic, 3D render, CGI, live action, motion blur, text
  C:\banyan-farm\ep2-b03-leafB-0813\b03-jobs-encode.json: "[{\"beat\": 4,\n  \"embeds\": \"C:\\\\banyan-farm\\\\ep2-b03-leafB-0813\\\\b03-embeds.pt\",\n  \"prompt_file\": \"C:\\\\banyan-farm\\\\ep2-b03-leafB-0813\\\\b03-motion-prompt.txt\",\n  \"negative_file\": \"C:\\\\banyan-farm\\\\ep2-b03-leafB-0813\\\\b03-negative.txt\"}]"
  C:\banyan-farm\ep2-b03-leafB-0813\b03-jobs-render.json: "[{\"beat\": 4,\n  \"seed\": 20260813,\n  \"embeds\": \"C:\\\\banyan-farm\\\\ep2-b03-leafB-0813\\\\b03-embeds.pt\",\n  \"init\": \"C:\\\\banyan-farm\\\\ep2-b03-leafB-0813\\\\b03-init-704x1280.png\",\n  \"out\": \"C:\\\\banyan-farm\\\\ep2-b03-leafB-0813\\\\03-bad-cover-LTX-leafB-0813.mp4\"}]"
steps:
- name: crop
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\ep2-b03-leafB-0813\cover_crop.py
  - --src
  - C:\banyan-farm\goblin-ipa-0812\out-b03-frozen\03-bad-cover-ipa-r0-w015-s1.png
  - --sha256
  - 2139ee6b4f89a8e68b13ef39735025b028bc1209258e22912cfbc404df432ef2
  - --out
  - C:\banyan-farm\ep2-b03-leafB-0813\b03-init-704x1280.png
  - --size
  - 704x1280
- name: encode
  argv:
  - C:\banyan-video\venv\Scripts\python.exe
  - C:\banyan-farm\banyan-city\pipeline\ltx_i2v.py
  - --stage
  - encode
  - --jobs
  - C:\banyan-farm\ep2-b03-leafB-0813\b03-jobs-encode.json
- name: render
  argv:
  - C:\banyan-video\venv\Scripts\python.exe
  - C:\banyan-farm\banyan-city\pipeline\ltx_i2v.py
  - --stage
  - render
  - --jobs
  - C:\banyan-farm\ep2-b03-leafB-0813\b03-jobs-render.json
  - --size
  - 704x1280
  - --frames
  - '121'
  - --fps
  - '24'
  - --guidance
  - '2.0'
  - --distilled-sigmas
  - --two-stage
  - --image-crf
  - '33'
  - --offload
  - sequential
  - --mode
  - production
  - --task
  - ep2-b03-leafB-0813
  - --worker
  - rtx5090
  - --bench-jsonl
  - C:\banyan-farm\ep2-b03-leafB-0813\bench-b03-leafB.jsonl
  - --bench-label
  - ep2-b03-leafB-0813
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - |
    import glob, hashlib, os, shutil
    dst = "C:/banyan-farm/courier-box/farm-out/ep2-b03-leafB"
    os.makedirs(dst, exist_ok=True)
    src = sorted(glob.glob("C:/banyan-farm/ep2-b03-leafB-0813/03-bad-cover-LTX-leafB-0813.mp4*")
                 + glob.glob("C:/banyan-farm/ep2-b03-leafB-0813/b03-init-704x1280.png")
                 + glob.glob("C:/banyan-farm/ep2-b03-leafB-0813/bench-b03-leafB.jsonl"))
    lines = []
    for f in src:
        shutil.copy2(f, dst)
        c = os.path.join(dst, os.path.basename(f))
        with open(c, "rb") as fh:
            h = hashlib.sha256(fh.read()).hexdigest()
        lines.append(h + "  " + os.path.basename(f))
    with open(os.path.join(dst, "ep2-b03-leafB-0813.sha256"), "w", newline="\n") as fh:
        fh.write("\n".join(lines) + "\n")
    print("published", len(src), "file(s) + manifest ->", dst)
    raise SystemExit(0 if len(src) >= 2 else 1)
artifacts:
- C:\banyan-farm\ep2-b03-leafB-0813\03-bad-cover-LTX-leafB-0813.mp4
- C:\banyan-farm\ep2-b03-leafB-0813\b03-init-704x1280.png
