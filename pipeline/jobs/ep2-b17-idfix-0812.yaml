# idfix wave 2026-08-12 ~03:0x: same refresh staging, frozen identity in
# the def. Dry 74/77 anchor intact - this beat had headroom, no lean needed.
# Beat 17 GOODBYE refresh, derived by sed from ep2-b11-refresh-0811.yaml at
# 2026-08-12 ~00:0x by the beats 14-20 lane. Nothing below describes beat 10 or
# 11: carrying a disclaimer forward is not the same as telling the truth, so the
# whole header was rewritten for this beat.
#
# HIS MANDATE, verbatim and covering this beat with no words of its own: "i
# looked at all the beats and you have to change pretty much all of them, do
# some regeneration, and do some evaluating of your own. train the system."
#
# -- WHAT THE WAVE-1 FRAMES ACTUALLY SHOW, READ BEFORE THE FIX WAS WRITTEN -----
# `review/ep2-picks/wave-0811/b17-s0..s3.jpg`, all four opened and looked at.
# THE FAULT ON THIS BEAT IS NOT A LOOK PROBLEM, IT IS A STORY BREAK:
#   * THREE OF FOUR HAVE THE SAPLING UPROOTED AND IN HIS HAND. s0: a tall leafy
#     staff he stands and leans on. s1: a plucked two-leaf sprig held up beside
#     his head. s2: a cutting carried like a torch. The sapling is the narrator
#     of this episode and it is rooted in a field; in three of four frames the
#     scavenger is walking off with it in his fist.
#   * FOUR OF FOUR DO NOT PERFORM THE BEAT. The line is "he stands, brushes
#     himself off and turns to go". In all four he is already standing, still,
#     facing camera. Nobody stands up, no dust is brushed, nobody turns.
#   * TWO OF FOUR ARE THE ROUND-3 REJECTION COMING BACK. The steward turned this
#     beat down on 2026-08-09 with "a hero walking off into a sunset, one of them
#     in a desert, and where is the tree". s2 is twilight -- blue sky, fireflies
#     -- and s0 is a dry sunset-yellow plain with bare hills.
#   * S3 IS THE DARK-FANTASY REGISTER AGAIN: glowing red eyes, red markings down
#     one cheek that read as blood, a heavy hooded cloak, and no sapling at all.
#   * ALREADY RIGHT AND THEREFORE NOT TOUCHED: outdoors in grass in 4/4, the
#     green skin and the patchwork cloak read, the count is one, and where the
#     sapling appears it is correctly TINY.
#
# -- THE FIX -------------------------------------------------------------------
# Staging kept from `authored` with ONE addition inside the staging sentence,
# named here rather than slipped in: `a tiny sapling` becomes `a tiny sapling
# rooted in the grass`. That is additive and not a rewording -- he still pushes
# himself up, still brushes dust off the patchwork cloak, still turns away from
# the sapling toward the open field -- and it is bought because three of four
# frames carried the narrator off. `authored_staged` reached for the same words
# on the same fault before this round existed. Riding with it, `no uprooted
# plant`: the guard beats taught that an affirmative alone does not hold against
# a strong prior, which is why the ratified register pairs `bare-headed` with
# `no helmet` rather than trusting either by itself.
#  * `no glowing eyes` -- the second verdict-specific ban, for s3, and the same
#    term the register spends as `no glowing visor` on the guards.
#  * THE EIGHT-TERM DARK BLOCK WHOLE, the one `ep2-b04-goblin-fieldbright-0811`
#    (commit cbcc716) proved on beat 04 against IDENTICAL SEEDS. `no dusk, no
#    sunset` are the two terms this beat is buying it for. They do NOT fight the
#    staging: the positive affirms `afternoon light warming toward amber`, and
#    banning dusk and sunset sharpens that line instead of contradicting it,
#    which is the opposite of the beat-11 case where the staging asked for the
#    long shadows the block would have banned.
#  * NO `bright morning light` and NO second field clause. The staging says
#    afternoon in its own words, and the draft already ends on `toward the open
#    field` -- this beat has never come back indoors, so beat 04's walls block
#    would be tokens spent on a fault that does not exist here.
#
# -- MEASURED ON THE BOX'S REAL CLIP TOKENIZER BEFORE ENQUEUE ------------------
# Positive 68/77 with STYLE ANCHOR PRESENT True, count tag `1boy` deriving and
# matching the draft, `text` negated True, DRY OK. Negative recipe 77 -> sent 77,
# every added term reaching the model: uprooted plant, glowing eyes and all eight
# lighting terms. NEGWARN, recorded rather than buried: the quality boilerplate
# goes (realistic skin texture, jpeg artifacts, deformed, extra limbs, blurry,
# low quality, signature, watermark, abstract, 3d render, photorealistic) and so
# do `shrubbery, bush, forest, full canopy, thick trunk, tall tree, large tree`.
# `mature tree` survives. THE DARK BLOCK WAS NOT NARROWED TO BUY THOSE BACK, and
# that is the one place this beat differs from the beat 15 refresh staged twenty
# minutes ago: beat 15's sapling keeps rendering as a houseplant, so its scale
# guard is load-bearing, while this beat draws a correctly tiny sapling and gets
# the position wrong. The round-3 note here was "where is the tree", not "the
# tree is too big". Stated as a risk before the frames exist, not after.
#
# -- ON THE DRAFTS FILE'S SHA -------------------------------------------------
# The sha attested below is the box copy as of THIS job's enqueue. This lane
# stages beats 19 and 20 after it and each scp appends a new variant key, so a
# later whole-file sha does not mean this beat's prompt moved: every edit is
# additive, `authored_b17_refresh` is byte-identical to the text measured above,
# and the harness reads the variant BY KEY and refuses outright when it is
# missing.
#
# WHY THIS IS ONE SAMPLE AND NOT A BATCH. CLAUDE.md, founder 2026-08-03: one
# sample per recipe change. This is one recipe change on one beat, four seeds, $0.
#
# SEPARATE OUT DIRECTORY, DELIBERATELY. The harness names files from the beat
# slug; this writes to `out-b17-idfix` and overwrites no earlier evidence.

id: ep2-b17-idfix-0812
task: ep2-b17-idfix-0812
node: 002b-first-citizen
beat: 17
runner: box
needs_gpu: true

# 48. box_runner.py sorts priority ASCENDING and takes ONE job at a time
# regardless -- sequential is the rule (CLAUDE.md, founder 2026-08-03) and
# batch-of-4 is banned. This lane's beats 14 and 15 went at 46 and 47 and have
# both finished; 19 and 20 follow at 49 and 50, so the lane lands in beat order
# if any of them stack. The ready AND running queues were both EMPTY when this
# was staged (`box_enqueue.py --list`, checked, not assumed) -- the card is idle
# again, which is the condition his mandate names.
priority: 48
max_attempts: 1
owner: beats 14-20 regeneration lane, 2026-08-11 (his wholesale-reject mandate)

# NO `gate:` KEY. One sample on a beat he has rejected wholesale, applying a
# register he has ratified and blocks already proven on other beats against
# identical seeds. It clears NOBODY's gate: the fourteen
# `ep2-b*-goblin-wave1.yaml` specs and the staged siblings keep theirs.

script_authority: 'Node 002b-first-citizen, live script `002b-t0-c`,
  `approved_by: founder`, `approved_on: 2026-08-03` (read from
  genomes/sapling/nodes/002b-first-citizen/leaves/002b-t0-c.yaml:17-18, the leaf the
  render gate itself reads, not copied from a sibling spec). His `approved_quote`
  verbatim: "approve 002b except.. we need to make SURE the fig growing on the sapling
  is mentioned, last time we generated 002b it just.. appeared. also, the sapling was
  able to flail a leaf in episode 1? why couldnt it do that in 002b?" Both
  `approval_conditions` are about beat 01''s fig and beat 16''s leaf; neither touches
  beat 17''s lighting, framing or staging. Beat 17 is GOODBYE, the shot in which the
  scavenger stands, brushes himself off and turns to leave the sapling behind.'

consumer: >-
  The founder's morning review page, as the regenerated beat 17 against
  `review/ep2-picks/wave-0811/b17-s0..s3.jpg` -- the four frames described in the
  header, three of them with the sapling pulled out of the ground and carried.
  Same beat, same four seeds, so the comparison is airtight. This job ranks
  nothing and picks nothing; which frames are good is R4's call.

success: >-
  Four PNGs at C:\banyan-farm\wave-goblin-prep\out-b17-idfix\17-goodbye-wave1-s0..3.png
  with their .yaml provenance sidecars written at render time (7.2), all eight
  declared as artifacts so a missing one fails rc=92 instead of passing quietly.
  Whether the frames are good is R4's call, not this job's and not a metric's.

why: >-
  Beat 17's wave-1 frames came back with the sapling uprooted and in his hand in 3
  of 4, nobody standing up or turning to go in 4 of 4, twilight in one and a dry
  sunset plain in another, and glowing red eyes with blood-red cheek markings in
  the last. This round roots the sapling in the grass affirmatively and bans the
  uprooted plant in the channel that binds, bans the glowing eyes, and sends beat
  04's eight-term dark block whole for the dusk and the sunset. Staging is
  otherwise verbatim and the character is untouched. Measured before enqueue:
  positive 68/77, style anchor intact, `1boy` deriving, negative recipe 77 -> sent
  77 with every added term reaching the model, DRY OK.

goblin_def: green skin
goblin_def_source: >-
  The founder, verbatim: "he is just a simple green goblin, nothing complex"
  (2026-08-10 ~12:15, re-stated 2026-08-11), plus his verdict on the frames drawn
  from it: "yeah, that looks like the right goblin" (2026-08-11 ~15:45, on
  `ep2-b15-goblin-simple-0811`; ledger `ep2-b15-goblin-simple-founder-verdict-0811`).
  `green skin` is the ONE attribute he named, and this round changes nothing about
  the character.

est_minutes: 2
needs:
- cuda
- vram20
- farm-venv

env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True

# The box's wave-drafts.yaml was scp'd from this repo and verified sha256-IDENTICAL
# (d7634da5718d989a5ed1b3041c67712d9d9e8b246383761722356fcae7c391f3) after the edit
# and before this job was queued -- the box printed that sha itself on the dry run
# quoted in the header, and it matches `shasum -a 256 pipeline/wave-drafts.yaml` in
# this repo. This is not a formality: `ep2-b08-refresh-0811` FAILED at its measure
# step (rc=1, 7 seconds, queue/failed/) because the box's copy at run time was not
# the copy its spec attested to. The harness REFUSES rather than falling back when a
# variant key is missing, so a stale copy fails for $0 instead of quietly
# re-rendering the old prompt under a new job id.
steps:
- name: measure
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '17'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b17_refresh
  - --dry
- name: sample
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '17'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b17_refresh
  - --out
  - C:\banyan-farm\wave-goblin-prep\out-b17-idfix
  - --task
  - ep2-b17-idfix-0812

# DURABILITY: every still this farm makes lands single-copy on a box nothing backs
# up. The courier runs `git add -A -- farm-out` and pushes on job_done, and
# farm-out is NOT gitignored, so publishing is a COPY and the runner stays
# byte-identical. allow_fail: a failed copy must never mark a good render failed.
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - |
    import glob, hashlib, os, shutil
    dst = "C:/banyan-farm/courier-box/farm-out/ep2-b17-idfix"
    os.makedirs(dst, exist_ok=True)
    src = sorted(glob.glob("C:/banyan-farm/wave-goblin-prep/out-b17-idfix/17-goodbye-wave1-s*.*"))
    lines = []
    for f in src:
        shutil.copy2(f, dst)
        # Hash the COPY, not the original: what this records has to be the bytes
        # that get committed, or the manifest attests to the wrong file.
        c = os.path.join(dst, os.path.basename(f))
        with open(c, "rb") as fh:
            h = hashlib.sha256(fh.read()).hexdigest()
        lines.append(h + "  " + os.path.basename(f))
        print("   ", os.path.basename(f), h[:16])
    # newline="\n" is load-bearing: Windows text mode rewrites \n to \r\n and
    # every filename then carries a trailing \r that shasum -c cannot open.
    with open(os.path.join(dst, "17-goodbye-refresh.sha256"), "w", newline="\n") as fh:
        fh.write("\n".join(lines) + "\n")
    print("published", len(src), "file(s) + manifest ->", dst)
    # 4 stills + 4 sidecars. Fewer means the render wrote less than it promised.
    raise SystemExit(0 if len(src) == 8 else 1)

artifacts:
- C:\banyan-farm\wave-goblin-prep\out-b17-idfix\17-goodbye-wave1-s0.png
- C:\banyan-farm\wave-goblin-prep\out-b17-idfix\17-goodbye-wave1-s1.png
- C:\banyan-farm\wave-goblin-prep\out-b17-idfix\17-goodbye-wave1-s2.png
- C:\banyan-farm\wave-goblin-prep\out-b17-idfix\17-goodbye-wave1-s3.png
- C:\banyan-farm\wave-goblin-prep\out-b17-idfix\17-goodbye-wave1-s0.yaml
- C:\banyan-farm\wave-goblin-prep\out-b17-idfix\17-goodbye-wave1-s1.yaml
- C:\banyan-farm\wave-goblin-prep\out-b17-idfix\17-goodbye-wave1-s2.yaml
- C:\banyan-farm\wave-goblin-prep\out-b17-idfix\17-goodbye-wave1-s3.yaml
