# EPISODE 2, BEAT 04 - r3 OF THE IDENTITY-FROZEN ROUND. One sample, four seeds,
# $0. 2026-08-12, the fixer lane.
#
# AN r3 BECAUSE THE r2 MEASURABLY FAILED, NOT BECAUSE THE LANE WANTED ANOTHER
# GUESS AT IT.
#
# THE CORRECTION THIS LANE OWES. After `ep2-b02-idfix-r2-0812` came back with
# clean crowns 4/4, this lane wrote that the pale dome and the sutures were ONE
# fault - the ban had removed both together - and propagated the ban alone to six
# beats. THAT WAS WRONG, and the wave now says why. Beat 02 is a MEDIUM shot.
# Beat 04 is an extreme close-up and beat 14 an intimate one, and at that size the
# negative reaches the stitches but cannot hold the scalp's COLOUR. Beat 11, a
# wide, never had the fault at all. It was one shot size, not one fault.
#
# BOTH FAILURES ARE MEASURED, NOT ASSUMED. The r2 sidecars were read: `scars` and
# `stitched skin` are both present in the negative that actually reached the
# model, and the frames still carry the pale scalp. The recipe was delivered and
# the recipe was insufficient.
#
# THE OTHER HALF IS ALSO MEASURED. The lead's `authored_b03_idfix_r2` sent `green
# scalp` as an AFFIRMATIVE with no skin ban at all, and its skull came back FULLY
# GREEN with no dome in both frames read. The affirmative holds the colour the ban
# cannot reach. THIS ROUND SENDS BOTH - the first recipe on this beat with a
# measured result behind each half.
#
# WHAT THE r2 DID AND DID NOT DO HERE: `ep2-b04-idfix-r3-0812` sent the skin ban
# and its frames STILL carry the cream dome over the upper skull, with X marks on the cheeks. The sutures are reduced; the pale scalp is not.
#
# The staging is untouched: the close-up, the darting eyes and the twitching ears are word for word the r2, and the walls ban is unchanged. `green scalp` is the only positive edit.
#
# BOX DRY-RUN, THE SHIPPED RECIPE: positive 69/77, anchor present, count tag '1boy', and both `scars` and `stitched skin`
# still sent in the negative.
#
# SEPARATE OUT DIRECTORY. Writes to `out-b04-idfix-r3`; overwrites neither the
# `_idfix` baseline nor the r2 this answers.

id: ep2-b04-idfix-r3-0812
task: ep2-b04-idfix-r3-0812
node: 002b-first-citizen
beat: 4
runner: box
needs_gpu: true

priority: 44
max_attempts: 1
owner: overnight fixer lane, 2026-08-12 (reading the identity-frozen wave and firing one r2 per faulty beat)

# NO `gate:` KEY. One sample moving one channel on a recipe he has ratified.

script_authority: 'Node 002b-first-citizen, live script `002b-t0-c`,
  `approved_by: founder`, `approved_on: 2026-08-03` (read from
  genomes/sapling/nodes/002b-first-citizen/leaves/002b-t0-c.yaml:17-18, the leaf the
  render gate itself reads). His `approved_quote` verbatim: "approve 002b except.. we
  need to make SURE the fig growing on the sapling is mentioned, last time we generated
  002b it just.. appeared. also, the sapling was able to flail a leaf in episode 1? why
  couldnt it do that in 002b?" Both `approval_conditions` are about beat 01''s fig and
  beat 16''s leaf; neither touches beat 4.'

consumer: >-
  Roman's next look at beat 4, as a THREE-WAY against the `_idfix` baseline and the r2 - same four seeds throughout. It ranks nothing and picks nothing; the taste call stays R4's.

success: >-
  Four PNGs at C:\banyan-farm\wave-goblin-prep\out-b04-idfix-r3\04-the-footnote-wave1-s0..3.png with their
  .yaml provenance sidecars written at render time (7.2), all eight declared as
  artifacts so a missing one fails rc=92 instead of passing quietly. Whether the
  frames are good is R4's call, not this job's and not a metric's.

why: >-
  One variable against `ep2-b04-idfix-r2-0812`: `green scalp` added to the positive. The skin ban, the staging, the seeds, steps (40), guidance (7.5), size and harness are all unchanged, so any difference is the affirmative that beat 03 proved.

goblin_def: green skin, bald head, patchwork cloak
goblin_def_source: >-
  The frozen identity, ledger `ep2-frozen-identities-0812`, passed byte-identical. NOT touched - `green scalp` is BEAT TEXT, not a definition change. That the definition is the underlying cause is argued from the beat-08 control in review/ep2-picks/wave2-needs-founder.md.

est_minutes: 2
needs:
- cuda
- vram20
- farm-venv

env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True

# The box's wave-drafts.yaml was scp'd from this repo and `findstr`-verified to
# carry `authored_b04_idfix_r3` before this job was queued, and the recipe below was
# dry-run on the box's real CLIP tokenizer. THE CHECK IS THE KEY, NOT THE
# WHOLE-FILE SHA: sibling lanes append their own variants tonight and every append
# moves the file sha without touching this variant. The harness REFUSES rather than
# falling back when a key is missing, so a stale copy fails for $0.
steps:
- name: measure
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '4'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b04_idfix_r3
  - --dry
- name: sample
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '4'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b04_idfix_r3
  - --out
  - C:\banyan-farm\wave-goblin-prep\out-b04-idfix-r3
  - --task
  - ep2-b04-idfix-r3-0812

# DURABILITY: every still this farm makes lands single-copy on a box nothing backs
# up. The courier runs `git add -A -- farm-out` and pushes on job_done, and
# farm-out is NOT gitignored, so publishing is a COPY and the runner stays
# byte-identical. allow_fail: a failed copy must never mark a good render failed.
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - |
    import glob, hashlib, os, shutil
    dst = "C:/banyan-farm/courier-box/farm-out/ep2-b04-idfix-r2"
    os.makedirs(dst, exist_ok=True)
    src = sorted(glob.glob("C:/banyan-farm/wave-goblin-prep/out-b04-idfix-r3/04-the-footnote-wave1-s*.*"))
    lines = []
    for f in src:
        shutil.copy2(f, dst)
        # Hash the COPY, not the original: what this records has to be the bytes
        # that get committed, or the manifest attests to the wrong file.
        c = os.path.join(dst, os.path.basename(f))
        with open(c, "rb") as fh:
            h = hashlib.sha256(fh.read()).hexdigest()
        lines.append(h + "  " + os.path.basename(f))
        print("   ", os.path.basename(f), h[:16])
    # newline="\n" is load-bearing: Windows text mode rewrites \n to \r\n and every
    # filename then carries a trailing \r that shasum -c cannot open.
    with open(os.path.join(dst, "04-the-footnote-idfix-r3.sha256"), "w", newline="\n") as fh:
        fh.write("\n".join(lines) + "\n")
    print("published", len(src), "file(s) + manifest ->", dst)
    # 4 stills + 4 sidecars. Fewer means the render wrote less than it promised.
    raise SystemExit(0 if len(src) == 8 else 1)

artifacts:
- C:\banyan-farm\wave-goblin-prep\out-b04-idfix-r3\04-the-footnote-wave1-s0.png
- C:\banyan-farm\wave-goblin-prep\out-b04-idfix-r3\04-the-footnote-wave1-s1.png
- C:\banyan-farm\wave-goblin-prep\out-b04-idfix-r3\04-the-footnote-wave1-s2.png
- C:\banyan-farm\wave-goblin-prep\out-b04-idfix-r3\04-the-footnote-wave1-s3.png
- C:\banyan-farm\wave-goblin-prep\out-b04-idfix-r3\04-the-footnote-wave1-s0.yaml
- C:\banyan-farm\wave-goblin-prep\out-b04-idfix-r3\04-the-footnote-wave1-s1.yaml
- C:\banyan-farm\wave-goblin-prep\out-b04-idfix-r3\04-the-footnote-wave1-s2.yaml
- C:\banyan-farm\wave-goblin-prep\out-b04-idfix-r3\04-the-footnote-wave1-s3.yaml
