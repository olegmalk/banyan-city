# EPISODE 2, BEAT 02 THE SPRINT — r2 OF THE IDENTITY-FROZEN ROUND. One sample,
# four seeds, $0. 2026-08-12, the fixer lane.
#
# WHAT THE FOUR `_idfix` FRAMES ACTUALLY SHOWED (farm-out/ep2-b02-idfix/, read
# frame by frame, recorded in review/ep2-picks/wave2-faults-fixer.yaml). The
# frozen identity LANDED: green skin 4/4, bald 4/4, patchwork cloak 4/4, one
# goblin 4/4, nothing pale. Two faults, and this round answers both.
#
# 1. THE PATCHWORK CLIMBED ONTO HIS HEAD, 4/4. Every frame sews the cloak into
#    the character: a tan graft sutured across the crown (s0), X-stitches on a
#    pale skull (s1), a vertical seam down the middle of the head (s3), red
#    stitch-scars on the cheeks (s0, s2, s3). He reads as Frankenstein surgery,
#    not as the goblin. This is the drift class his own beat-04 sentence named
#    ("character consistency"), arriving through the word `patchwork`.
#    CHANNEL: negative ban, worded so it cannot negate what the positive asserts.
#    The positive says `patchwork cloak`, so the ban names SKIN and never the
#    bare word `stitches`: `no scars, no stitched skin`.
# 2. THE SAPLING HE HIDES BEHIND IS IN NO FRAME, 0/4. The sentence sent asks for
#    a tiny sapling with "its thin stem in front of him" and "his face peeking
#    around it". s0 and s2 replace the one sapling with a whole seedling bed, s1
#    parks a stem at the right frame edge clear of him, s3 buries him in grass.
#    CHANNEL: omission -> affirmative, moved EARLY. The stem goes into the third
#    clause, ahead of the action, the placement `authored_b04_fieldban` proved
#    against the house drift. `Medium shot` pays for it. The count tag is checked
#    and not assumed: `A small goblin boy` still heads the sentence and the
#    insert lands after the first comma-clause, so `_tag_from_clause` still
#    derives `1boy` — the box dry-run below prints it.
#
# THIS IS THE ONE r2 THE SAPLING-ADJACENCY LIMIT GETS. The stem-beside-figure
# failure is old (commit bdcb1a2a read the sapling as absent from all eight
# goblin frames). If it is still absent here, beat 02 goes to
# review/ep2-picks/wave2-needs-founder.md and the lane moves on rather than
# spending a third round on a known model limit.
#
# ── THE NEGATIVE WAS MEASURED, NOT ASSUMED: THREE DRY RUNS, $0 EACH ───────────
# The `_idfix` negative already sent 77 of 77, so two new terms cannot simply be
# added — they evict the TAIL, and the tail on this beat is `female goblin`,
# `elf` and the whole plant-fusion tier. Two cheaper recipes were measured and
# REJECTED before this one, and both are worse than the round they replace:
#   a) retire `no grabbing` + `no pulling`  -> sent 75, silently loses `mature
#      tree`. On a beat whose whole fix is a TINY sapling reading correctly,
#      dropping the anti-big-tree guard is the wrong trade.
#   b) (a) plus `no mature tree` re-added   -> sent 75, buys the tree back and
#      loses `photorealism` instead.
#   c) SHIPPED: retire the whole grab tier (`grabbing`, `pulling`, `uprooting`).
#      Recipe 77 -> sent 77, and NO NEGWARN AT ALL. Strictly more guard than the
#      round it replaces: `mature tree` AND `large tree` AND `photorealism` all
#      survive alongside the two new bans, and the plant-fusion tier, `female
#      goblin` and `elf` are untouched.
# RETIRING THE GRAB TIER IS PAID FOR, NOT WISHED AWAY: the positive now pins the
# hands affirmatively ("cowers with both hands to his chest"), and all four
# `_idfix` frames already drew hands-to-chest 4/4 with no grabbing, so the ban
# was not the thing holding that.
#
# BOX DRY-RUN, THE SHIPPED RECIPE, on the box's real CLIP tokenizer:
#   positive 69/77   STYLE ANCHOR PRESENT: True   count tag '1boy'
#   'text' negated: True   negative recipe 77 -> sent 77 (budget 77)
#   NEG(sent): text, mature tree, large tree, girl, child, scars, stitched skin,
#     dark, night, dusk, sunset, dim lighting, moody lighting, low key, shadows
#     dominant, photorealism, leaf on head, plant girl, alraune, monster girl,
#     flower on head, head wreath, hair ornament, leaf hair ornament, plant hair,
#     female goblin, elf
#
# SEPARATE OUT DIRECTORY, DELIBERATELY. `out-b02-idfix` and `out-b02-hide` hold
# evidence this round is read against. This writes to `out-b02-idfix-r2` and
# overwrites none of it.

id: ep2-b02-idfix-r2-0812
task: ep2-b02-idfix-r2-0812
node: 002b-first-citizen
beat: 2
runner: box
needs_gpu: true

# box_runner.py sorts priority ASCENDING and takes ONE job at a time regardless —
# sequential is the rule (CLAUDE.md, founder 2026-08-03) and batch-of-4 is banned.
priority: 44
max_attempts: 1
owner: overnight fixer lane, 2026-08-12 (reading the identity-frozen wave and firing one r2 per faulty beat)

# NO `gate:` KEY. One sample on a recipe he has ratified, moving two channels
# that are separately visible in the frames. It clears nobody's gate.

script_authority: 'Node 002b-first-citizen, live script `002b-t0-c`,
  `approved_by: founder`, `approved_on: 2026-08-03` (read from
  genomes/sapling/nodes/002b-first-citizen/leaves/002b-t0-c.yaml:17-18, the leaf the
  render gate itself reads). His `approved_quote` verbatim: "approve 002b except.. we
  need to make SURE the fig growing on the sapling is mentioned, last time we generated
  002b it just.. appeared. also, the sapling was able to flail a leaf in episode 1? why
  couldnt it do that in 002b?" Both `approval_conditions` are about beat 01''s fig and
  beat 16''s leaf; neither touches beat 02''s staging. Beat 02 is THE SPRINT, the
  sprint/dive to cover behind the sapling.'

consumer: >-
  Roman's next look at beat 02, as a PAIR against the `out-b02-idfix` frames — same
  four seeds, so the stitched scalp and the missing stem are both attributable by
  eye. It ranks nothing and picks nothing; the taste call stays R4's.

success: >-
  Four PNGs at C:\banyan-farm\wave-goblin-prep\out-b02-idfix-r2\02-the-sprint-wave1-s0..3.png
  with their .yaml provenance sidecars written at render time (7.2), all eight
  declared as artifacts so a missing one fails rc=92 instead of passing quietly.
  Whether the frames are good is R4's call, not this job's and not a metric's.

why: >-
  Two faults read off the `_idfix` frames, each answered in the channel that binds:
  a skin-and-head ban for the stitched scalp (worded so it cannot negate the
  `patchwork cloak` the positive asserts), and an early affirmative for the absent
  sapling stem. Seeds, steps (40), guidance (7.5), size and harness (sha256
  7efd0b96) are unchanged, so any difference in these frames is those two edits.

goblin_def: green skin, bald head, patchwork cloak
goblin_def_source: >-
  The frozen identity, ledger `ep2-frozen-identities-0812`. `green skin` is the one
  attribute the founder named ("he is just a simple green goblin, nothing complex",
  2026-08-10 ~12:15); BALD is his ruling; the patchwork cloak is the script''s. This
  round does not touch the definition — it is passed byte-identical to the `_idfix`
  round it answers.

est_minutes: 2
needs:
- cuda
- vram20
- farm-venv

env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True

# The box's wave-drafts.yaml was scp'd from this repo and `findstr`-verified to
# carry `authored_b02_idfix_r2` before this job was queued. THE CHECK IS THE KEY,
# NOT THE WHOLE-FILE SHA: sibling lanes append their own variants tonight and
# every append moves the file sha without touching this variant. The harness
# REFUSES rather than falling back when a variant key is missing, so a stale copy
# fails for $0 instead of quietly re-rendering the old prompt under a new job id.
# The `measure` step re-runs the dry check on the box at run time for the same $0.
steps:
- name: measure
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '2'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b02_idfix_r2
  - --dry
- name: sample
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '2'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b02_idfix_r2
  - --out
  - C:\banyan-farm\wave-goblin-prep\out-b02-idfix-r2
  - --task
  - ep2-b02-idfix-r2-0812

# DURABILITY: every still this farm makes lands single-copy on a box nothing backs
# up. The courier runs `git add -A -- farm-out` and pushes on job_done, and
# farm-out is NOT gitignored, so publishing is a COPY and the runner stays
# byte-identical. allow_fail: a failed copy must never mark a good render failed.
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - |
    import glob, hashlib, os, shutil
    dst = "C:/banyan-farm/courier-box/farm-out/ep2-b02-idfix-r2"
    os.makedirs(dst, exist_ok=True)
    src = sorted(glob.glob("C:/banyan-farm/wave-goblin-prep/out-b02-idfix-r2/02-the-sprint-wave1-s*.*"))
    lines = []
    for f in src:
        shutil.copy2(f, dst)
        # Hash the COPY, not the original: what this records has to be the bytes
        # that get committed, or the manifest attests to the wrong file.
        c = os.path.join(dst, os.path.basename(f))
        with open(c, "rb") as fh:
            h = hashlib.sha256(fh.read()).hexdigest()
        lines.append(h + "  " + os.path.basename(f))
        print("   ", os.path.basename(f), h[:16])
    # newline="\n" is load-bearing: Windows text mode rewrites \n to \r\n and
    # every filename then carries a trailing \r that shasum -c cannot open.
    with open(os.path.join(dst, "02-the-sprint-idfix-r2.sha256"), "w", newline="\n") as fh:
        fh.write("\n".join(lines) + "\n")
    print("published", len(src), "file(s) + manifest ->", dst)
    # 4 stills + 4 sidecars. Fewer means the render wrote less than it promised.
    raise SystemExit(0 if len(src) == 8 else 1)

artifacts:
- C:\banyan-farm\wave-goblin-prep\out-b02-idfix-r2\02-the-sprint-wave1-s0.png
- C:\banyan-farm\wave-goblin-prep\out-b02-idfix-r2\02-the-sprint-wave1-s1.png
- C:\banyan-farm\wave-goblin-prep\out-b02-idfix-r2\02-the-sprint-wave1-s2.png
- C:\banyan-farm\wave-goblin-prep\out-b02-idfix-r2\02-the-sprint-wave1-s3.png
- C:\banyan-farm\wave-goblin-prep\out-b02-idfix-r2\02-the-sprint-wave1-s0.yaml
- C:\banyan-farm\wave-goblin-prep\out-b02-idfix-r2\02-the-sprint-wave1-s1.yaml
- C:\banyan-farm\wave-goblin-prep\out-b02-idfix-r2\02-the-sprint-wave1-s2.yaml
- C:\banyan-farm\wave-goblin-prep\out-b02-idfix-r2\02-the-sprint-wave1-s3.yaml
