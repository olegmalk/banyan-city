id: ep2-b04-canonmotion-s2-0822
task: ep2-b04-canonmotion-s2-0822
node: 002b-first-citizen
beat: 4
runner: box
priority: 14
needs_gpu: true
max_attempts: 1
owner: the per-beat iteration lane, 2026-08-22
consumer: A SECOND FINISHED OPTION for beat 04, so the founder picks between two clips of his own design
  rather than approving the only one that exists. The first is already staged on review/ep2-beats-0821,
  trimmed to the slot.
success: 'ONE 704x1280 105-frame 24fps mp4 that is a DIFFERENT good take. E1 he leans out and comes back
  inside the clip -- the pull-back is the joke and it has to land; E2 the frame does not pull back and
  he does not shrink; E3 identity holds to the last frame: bald, big lateral pointed ears, off-white eyes
  with slit pupils, mandarin collar, boots. If it is worse than seed 1 it is simply not offered.'
why: 'THE FOUNDER''S NOTE ON BEAT 04 IS ANSWERED AND THE ANSWER IS A SAMPLE OF ONE. ''Wouldn''t be bad
  if he didnt look so chibi in this'' is the take in the cut -- a teal head filling the frame with red
  eyes and no ears. The canon-motion clip is his design instead and scored 6 of 7, and it is now trimmed
  to the slot and staged. But one clip is not a selection, and his 08-21 amendment asks for a selection.
  ONE VARIABLE, and it is the seed: this is the same plate, the same wording and the same recipe that
  produced the take he is being shown.'
est_minutes: 9
needs:
- cuda
- vram20
- video-venv
env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
payload:
  C:\banyan-farm\ep2-b04-canonmotion-s2-0822\cover_crop.py: "#!/usr/bin/env python3\n\"\"\"Cover-crop\
    \ an asserted source image to an exact WxH. No model, no network.\"\"\"\nimport argparse, hashlib,\
    \ os, sys\nfrom PIL import Image\n\nap = argparse.ArgumentParser()\nap.add_argument(\"--src\", required=True)\n\
    ap.add_argument(\"--sha256\", default=\"\", help=\"asserted before anything is written\")\nap.add_argument(\"\
    --out\", required=True)\nap.add_argument(\"--size\", default=\"704x1280\")\na = ap.parse_args()\n\n\
    if not os.path.isfile(a.src):\n    sys.exit(\"!! src not found: %s\" % a.src)\nh = hashlib.sha256()\n\
    with open(a.src, \"rb\") as fh:\n    for chunk in iter(lambda: fh.read(1 << 20), b\"\"):\n       \
    \ h.update(chunk)\nhave = h.hexdigest()\nif a.sha256 and have != a.sha256:\n    sys.exit(\"!! SRC\
    \ SHA MISMATCH -- refusing.\\n   want %s\\n   have %s\" % (a.sha256, have))\n\nW, H = (int(v) for\
    \ v in a.size.split(\"x\"))\nim = Image.open(a.src).convert(\"RGB\")\nsw, sh = im.size\nscale = max(W\
    \ / float(sw), H / float(sh))\nnw, nh = int(round(sw * scale)), int(round(sh * scale))\nim = im.resize((nw,\
    \ nh), Image.LANCZOS)\nleft, top = (nw - W) // 2, (nh - H) // 2\nim = im.crop((left, top, left + W,\
    \ top + H))\nos.makedirs(os.path.dirname(os.path.abspath(a.out)) or \".\", exist_ok=True)\nim.save(a.out)\n\
    print(\"src %dx%d sha %s\" % (sw, sh, have), flush=True)\nprint(\"cover-crop scale=%.4f -> %dx%d,\
    \ centre box (%d,%d)\" % (scale, nw, nh, left, top), flush=True)\nprint(\"WROTE %s\" % a.out, flush=True)"
  C:\banyan-farm\ep2-b04-canonmotion-s2-0822\b04-motion-prompt.txt: '2D anime, hand-drawn cel animation,
    static locked framing, the frame never moves, flat cel shading, clean ink linework, anime key art.
    Subject already in frame: ONE small green goblin alone, bald, large pointed ears, off-white eyes with
    narrow vertical slit pupils, mandarin-collar sage shirt, dark shorts, dark boots. His face, ears,
    skin colour and clothes DO NOT CHANGE for the whole clip. He is hunched over, leaning out sideways,
    in tall grass, full body. THE ACTION: he leans out sideways from behind the trunk, looks, and pulls
    straight back -- out, look, back -- the pull-back is the joke and must land. HALFWAY THROUGH he is
    leaned out sideways at full extension, looking, not yet back.'
  C:\banyan-farm\ep2-b04-canonmotion-s2-0822\b04-negative.txt: second face, second goblin, two goblins,
    extra head, disembodied head, crowd, child, chibi, baby, round head, different face, changing face,
    morphing, melting face, featureless face, blank face, losing facial features, skin colour change,
    clothes changing, cloak, hood, patchwork, camera pan, camera zoom, camera shake, text, watermark
  C:\banyan-farm\ep2-b04-canonmotion-s2-0822\b04-jobs-encode.json: '[{"beat": 20, "embeds": "C:\\banyan-farm\\ep2-b04-canonmotion-s2-0822\\b04-embeds.pt",
    "prompt_file": "C:\\banyan-farm\\ep2-b04-canonmotion-s2-0822\\b04-motion-prompt.txt", "negative_file":
    "C:\\banyan-farm\\ep2-b04-canonmotion-s2-0822\\b04-negative.txt"}]'
  C:\banyan-farm\ep2-b04-canonmotion-s2-0822\b04-jobs-render.json: '[{"beat": 20, "seed": 20260845, "embeds":
    "C:\\banyan-farm\\ep2-b04-canonmotion-s2-0822\\b04-embeds.pt", "init": "C:\\banyan-farm\\ep2-b04-canonmotion-s2-0822\\b04-init-704x1280.png",
    "out": "C:\\banyan-farm\\ep2-b04-canonmotion-s2-0822\\04-evidence-LTX-ep2-b04-canonmotion-s2-0822.mp4"}]'
steps:
- name: crop
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\ep2-b04-canonmotion-s2-0822\cover_crop.py
  - --src
  - C:\banyan-farm\courier-box\farm-out\ep2-b04-canon-w2-0821\ep2-b04-canon-w2-0821-ipahead.png
  - --sha256
  - 98e20fd2f39bd5afdcc62b5945fda1b1c03167d7420a2ae76ffcab5dae280816
  - --out
  - C:\banyan-farm\ep2-b04-canonmotion-s2-0822\b04-init-704x1280.png
  - --size
  - 704x1280
- name: encode
  argv:
  - C:\banyan-video\venv\Scripts\python.exe
  - C:\banyan-farm\banyan-city\pipeline\ltx_i2v.py
  - --stage
  - encode
  - --jobs
  - C:\banyan-farm\ep2-b04-canonmotion-s2-0822\b04-jobs-encode.json
- name: render
  argv:
  - C:\banyan-video\venv\Scripts\python.exe
  - C:\banyan-farm\banyan-city\pipeline\ltx_i2v.py
  - --stage
  - render
  - --jobs
  - C:\banyan-farm\ep2-b04-canonmotion-s2-0822\b04-jobs-render.json
  - --size
  - 704x1280
  - --frames
  - '105'
  - --fps
  - '24'
  - --guidance
  - '2.0'
  - --distilled-sigmas
  - --two-stage
  - --image-crf
  - '10'
  - --offload
  - sequential
  - --mode
  - production
  - --task
  - ep2-b04-canonmotion-s2-0822
  - --worker
  - rtx5090
  - --bench-jsonl
  - C:\banyan-farm\ep2-b04-canonmotion-s2-0822\bench-ep2-b04-canonmotion-s2-0822.jsonl
  - --bench-label
  - ep2-b04-canonmotion-s2-0822
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - "import glob, hashlib, os, shutil\n# farm-out is NOT gitignored: the runner pushes whatever lands\
    \ here itself,\n# so a clip that reaches this dir survives without any change to the runner.\ndst\
    \ = \"C:/banyan-farm/courier-box/farm-out/ep2-b04-canonmotion-s2-0822\"\nos.makedirs(dst, exist_ok=True)\n\
    src = sorted(glob.glob(\"C:/banyan-farm/ep2-b04-canonmotion-s2-0822/04-evidence-LTX-ep2-b04-canonmotion-s2-0822.mp4*\"\
    )\n             + glob.glob(\"C:/banyan-farm/ep2-b04-canonmotion-s2-0822/b04-init-704x1280.png\")\n\
    \             + glob.glob(\"C:/banyan-farm/ep2-b04-canonmotion-s2-0822/bench-ep2-b04-canonmotion-s2-0822.jsonl\"\
    ))\nlines = []\nfor f in src:\n    shutil.copy2(f, dst)\n    c = os.path.join(dst, os.path.basename(f))\n\
    \    with open(c, \"rb\") as fh:\n        h = hashlib.sha256(fh.read()).hexdigest()\n    lines.append(h\
    \ + \"  \" + os.path.basename(f))\nwith open(os.path.join(dst, \"ep2-b04-canonmotion-s2-0822.sha256\"\
    ), \"w\", newline=\"\\n\") as fh:\n    fh.write(\"\\n\".join(lines) + \"\\n\")\nprint(\"published\"\
    , len(src), \"file(s) + manifest ->\", dst)\n# >=2 so a run that wrote the init but no clip cannot\
    \ pass silently.\nraise SystemExit(0 if len(src) >= 2 else 1)\n"
artifacts:
- C:\banyan-farm\ep2-b04-canonmotion-s2-0822\04-evidence-LTX-ep2-b04-canonmotion-s2-0822.mp4
script_authority: 'Node 002b-first-citizen, live script `002b-t0-c`, `approved_by: founder`, `approved_on:
  2026-08-03`. The line is untouched by this job. Beat 04''s staging is quoted in `script_line` from jerry_canon_0821.WAVE[4],
  which is the same node text the canon plate under this clip was drawn against. NOTE ON PROVENANCE: this
  spec''s parent is a BEAT 20 job and both of these keys cross derive_spec''s allow-list as structure
  -- carried unedited they would have had beat 04 claiming beat 16''s restaging as its authority, so they
  are re-stated here rather than inherited.'
script_line: THE FOOTNOTE -- 'leans out from behind the trunk to look, and pulls straight back the moment
  he has looked'. THE PEEK, the founder's own restaging pick (A, 2026-08-20). The eyes are the whole beat.
failure_predicted_in_advance: 'THE DRAW IS WORSE, or the one gap seed 1 has gets bigger: with no trunk
  in frame there is nothing to lean out FROM, so the peek can read as a stumble. A different seed can
  make that worse as easily as better. The cost of finding out is nine minutes on a card that would otherwise
  be idle, and the downside is a clip nobody is shown.'
the_one_variable: THE SEED. Everything else -- plate, prompt, negative, size, frames, fps, guidance, sampler,
  sigmas, two-stage, crf, offload -- is the judged clip's.
derivation:
  parent: pipeline/jobs/ep2-b04-canonmotion-0821.yaml
  by: pipeline/file_beatfix_0822.py
  method: ALLOW-LIST, not a deny-list. Only the keys pipeline/derive_spec.py names as structural cross
    from the parent; every other key -- verdicts, picks, sweeps, fired fail-modes, cut preferences, notes,
    however named or renamed -- stops at the boundary by construction. A verdict can arrive under any
    key name, so the closed set is what a runner and its guards read, not what a regex can enumerate.
  carried_structural_keys:
  - artifacts
  - beat
  - env
  - est_minutes
  - id
  - max_attempts
  - needs
  - needs_gpu
  - node
  - payload
  - priority
  - runner
  - script_authority
  - script_line
  - steps
  - task
  authored_fresh_by_the_caller:
  - consumer
  - failure_predicted_in_advance
  - owner
  - success
  - the_one_variable
  - why
  keys_the_parent_had_that_did_NOT_cross:
  - bar
  - derivation
  - failure_predicted_in_advance
  - identity_and_wardrobe_source
  - one_sample_rule
  - plate_provenance
  - post_ship_patch
  - the_one_variable
  - the_rung_this_is_one_variable_from
  - trim_and_hold_plan
  - verdict_canonmotion_0821
  retokened:
  - canonmotion-0821 -> canonmotion-s2-0822
  - ep2-b04-canonmotion-0821 -> ep2-b04-canonmotion-s2-0822
  overrides_applied_and_asserted:
    key:est_minutes:
    - key est_minutes = 9
    key:priority:
    - key priority = 14
    seed:
    - payload b04-jobs-render.json (20260904 -> 20260845, re-parsed)
  seed: 20260845
