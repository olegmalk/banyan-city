# 2026-08-18, BEAT 08 COMPOSITE/INIT LANE. ONE SAMPLE, ONE SEED.
# Authored by pipeline/author_b08_gesture_0818.py -- edit that, not this.
#
# ============================================================================
# THE RULING THIS EXECUTES
# ============================================================================
# Steward ruling, 2026-08-18: BEAT 08'S WORDING LEVER IS FALSIFIED and the route
# is composite/init. The falsification is on the record and is not a hunch --
# every text-to-image round on this beat returned a COLOSSUS, one figure towering
# over the other, and the round that deleted the sky to remove the cue returned
# one anyway. What an init buys is the thing words could not hold: the pair,
# their scale relative to each other, and ONE GROUND PLANE, all by construction.
#
# §6 IS SATISFIED: node 002b-first-citizen carries `approved_by: founder`,
# `approved_on: 2026-08-03`.
#
# ============================================================================
# WHY THIS INIT AND NOT A NEW COMPOSITE
# ============================================================================
# farm-out/ep2-b0708-twofig-mac-0815/08-inside-him-twofig-mac-r3-s0.png, drawn
# $0 on an M1 Pro on 2026-08-15. The board's own record says it "holds THE RIGHT
# PAIR for beat 08 (an adult guard and the adult goblin, same depth plane,
# adjacent, belly reachable by an extended arm, board face clean)", and the
# figure-count objection against it was WITHDRAWN on 2026-08-16 when the count
# was ruled from the script: beat 08 needs TWO figures, Guard 2 and the
# scavenger, not three.
#
# IT WAS OPENED BEFORE THIS SPEC WAS WRITTEN, not taken on the record's word.
# What is actually in the frame: a bald human in a brown cloak and tan robe
# standing on grass beside a green adult goblin in a teal robe, both whole
# figures, both feet on the same ground, sky and hills behind, neither towering
# over the other. The record is accurate.
#
# WHAT IS NOT IN IT, STATED PLAINLY BECAUSE THE BAR TURNS ON IT: the action.
# The guard holds his clipboard UP at chest height in both hands and nobody is
# pointing at anything. So B1, B2, B3 and B5 of the bar below are carried by the
# init and this render only has to not break them; B4 -- the clipboard DOWN and
# the point to the BELLY -- is the one clause the sampler is being asked for.
#
# ============================================================================
# THE ODDS, STATED BEFORE THE RENDER AND NOT AFTER
# ============================================================================
# 0.30 IS THE FINISH BAND, NOT THE ADD BAND, and this job asks it for something
# close to an addition. pipeline/composite-init-pattern.md §2: strength runs
# int(steps x strength), so 40 x 0.30 = 12 actual steps, and the working window
# is 0.2-0.35 -- above it "the sampler stops FINISHING and starts INVENTING",
# which is the failure the composite exists to avoid. Everywhere the 0.30 recipe
# has passed (beat 01's leaf count, 8 of 8; the bark boards on beats 06 and 10),
# the STRUCTURE was composited in first with plain image processing and the
# sampler was only asked to make it look drawn. Here there is no structural
# composite: the lowered board and the pointing arm would have to arrive from 12
# steps of denoising inside a mask.
#
# SO G1 -- THE CLIPBOARD STAYS RAISED -- IS THE PREDICTED OUTCOME, and it is
# named here before the render rather than discovered after it.
#
# IT IS STILL THE RIGHT FIRST SAMPLE, for two reasons. It is five minutes and $0
# on an empty card, against a beat with NO VERDICT YET whose wording lever is
# spent. And a FAIL narrows the route usefully: if the pair, the scale and the
# ground plane survive 0.30 untouched while only the gesture fails to arrive,
# the next step is a structural composite of the arm and the lowered board --
# which is a real, costed piece of work nobody should start on a guess.
#
# THE AMBIGUITY IS DECLARED IN ADVANCE. A FAIL here does NOT distinguish "the
# composite/init route cannot reach beat 08's staging" from "0.30 finishes and
# does not add". One sample cannot separate them and this spec will not be
# quoted as though it did.
#
# NOT THE SAME BEAT-08 WALL AS §9. pipeline/composite-init-pattern.md §9 records
# that beat 08 broke the pattern -- but that was the MATERIAL half, seven samples
# trying to get BARK onto a board face, and its own finding is that "the GEOMETRY
# half transferred completely" on three boards, tracking a traced edge to within
# 1.8px. This job asks for geometry, not material. §9 is not evidence against it.
#
# ============================================================================
# THE MASK, MEASURED AND LOOKED AT
# ============================================================================
# Ellipse 445,620,335,165 on the 832x1216 init = x 110-780, y 455-785, 22% of
# the frame. Drawn over the plate and opened on 2026-08-18 before this spec was
# filed. It covers: the guard's two hands and the whole clipboard, the gap
# between the men, the goblin's outstretched hand, and his belly and sash. It
# CLEARS both faces, both lower robes and all of the ground, so neither figure's
# identity nor the single ground plane is inside the region being redrawn. The
# geometry is the steward's and is the first thing a correction should move.
#
# ============================================================================
# GUARDS
# ============================================================================
# No motion here, so the plate and refs checks do not apply (job_animates reads
# the argv for ltx_i2v and finds none) -- and no plate_ack is used anywhere.
# The init is pinned by sha256 twice over: the fetch step REFUSES on mismatch
# before writing a byte, and inpaint_fruit.py asserts --init-sha256 again before
# a model is loaded, so a picture that changed between machines cannot be
# restyled by accident. The --dry-run step writes the mask and exits before any
# weight is touched. Every --out is an absolute Windows path. Payload dir and
# publish source are the same declared dir and both are named in argv. There is
# no allow_fail in this spec: a publish that cannot find all six files is a FAIL.
#
# ONE SEED. No sweep, no second strength, no wording variant. inpaint_fruit.py
# is INLINED byte-for-byte from the repo copy rather than referenced out of the
# box's checkout, which can lag -- its sha is in the authoring script's output.
#
# $0. Local card, no provider, no paid engine.
id: ep2-b08-twofig-gesture-0818
task: ep2-b08-twofig-gesture-0818
node: 002b-first-citizen
beat: 8
runner: box
priority: 36
needs_gpu: true
max_attempts: 1
sample: true
owner: beat 08 composite/init lane, 2026-08-18
consumer: 'Beat 08''s plate, for its cut slot in the episode 2 assembly. The beat has NO VERDICT YET in
  review/ep2-picks/done-definitions.yaml and its wording lever is spent. This is the first sample down
  the route the steward ruled on 2026-08-18, and its result decides one thing: whether beat 08''s staging
  can be reached by restyling an init that already holds the pair, or whether the gesture has to be composited
  in structurally the way beat 01''s leaf count was. Judged by opening the png against the bar this spec
  pre-registers.'
success: 'One 832x1216 png, its mask and its provenance sidecar. PASS requires ALL FIVE, pre-registered
  from review/ep2-picks/done-definitions.yaml beats.''08'' done_when as amended by figure_count_ruled_from_the_script_0817:
  B1 TWO FIGURES ON ONE GROUND PLANE -- the guard and the goblin standing on the same grass, feet at a
  consistent depth, which the init already satisfies and this render must not break; B2 THE GUARD READS
  ADULT; B3 THE GOBLIN READS ADULT and is the same green adult he is in the init -- not a child, not a
  chibi, not a second character; B4 THE CLIPBOARD IS DOWN and the POINT GOES TO THE BELLY, both legible,
  which is the beat''s own action and the only clause the sampler is being asked for; B5 NO COLOSSUS --
  neither figure towers over the other, which is the fault every wording round returned and the whole
  reason the init carries the scale. FAIL is any of: G1 the clipboard stays raised (the predicted outcome
  -- 0.30 is the FINISH band, and a lowered board plus a new pointing arm is an ADDITION); G2 a second
  board or a third arm appears, which is the founder''s own named defect from the beat 10 plate lane;
  G3 either figure''s identity changes; G4 the ground plane splits or a figure floats; G5 a colossus returns.
  A partial is a FAIL.'
why: 'The card is EMPTY -- ready/ holds only HOLD and DUP files, running/ and backlog/ were both empty
  when this was filed. Five minutes, $0, no provider, no new plate campaign. The init already exists and
  cost nothing: it was drawn on a Mac on 2026-08-15 and its figure-count objection was WITHDRAWN on 2026-08-16,
  so the cheapest sample on this route is a restyle over a picture we already have rather than a new composite
  nobody has written yet.'
est_minutes: 5
needs:
- cuda
- vram20
env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
payload:
  C:\banyan-farm\b08gesture-0818\fetch_init.py: "#!/usr/bin/env python3\n\"\"\"Fetch beat 08's two-figure\
    \ init and REFUSE on any sha mismatch.\n\nNo model, no GPU, no spend. The bytes are the ones on origin/main,\
    \ so the sha256\nasserted here is verifiable against the repo by anyone who clones it. The plate\n\
    was drawn on a Mac on 2026-08-15, so it is NOT on the box's courier worktree --\nthe courier only\
    \ ever contains what the box itself produced.\n\"\"\"\nimport hashlib, os, sys, urllib.request\n\n\
    OUT = r\"C:\\banyan-farm\\b08gesture-0818\"\nRAW = (\"https://raw.githubusercontent.com/olegmalk/banyan-city/main/\"\
    \n       \"farm-out/ep2-b0708-twofig-mac-0815/\")\nUA = {\"User-Agent\": \"banyan-city-b08-gesture/1.0\
    \ (albert.numbro@gmail.com)\"}\nNAME = \"08-inside-him-twofig-mac-r3-s0.png\"\nWANT = \"d9cd8b1a4642a60a3362d07feef30b47999bbe988e4ef52d047d1f4d06113238\"\
    \n\nos.makedirs(OUT, exist_ok=True)\nraw = urllib.request.urlopen(\n    urllib.request.Request(RAW\
    \ + NAME, headers=UA), timeout=120).read()\nhave = hashlib.sha256(raw).hexdigest()\nif have != WANT:\n\
    \    sys.exit(\"!! SHA MISMATCH for %s -- refusing.\\n   want %s\\n   have %s\"\n             % (NAME,\
    \ WANT, have))\nwith open(os.path.join(OUT, NAME), \"wb\") as fh:\n    fh.write(raw)\nprint(\"fetched\
    \ %s %d bytes sha %s OK\" % (NAME, len(raw), have), flush=True)\n"
  C:\banyan-farm\b08gesture-0818\inpaint_fruit.py: "#!/usr/bin/env python3\nr\"\"\"Paint ONE object into\
    \ ONE masked region of an approved plate. SDXL, $0, local.\n\nWHY THIS EXISTS. Beat 01 asks for \"\
    one small round green fruit hanging from the\nstem\" and the prompt cannot deliver it: 0 of 24 across\
    \ the wording rounds, and\n0 of 12 in round 8's img2img -- which is the decisive one, because r8's\
    \ init\ncarried no fruit at all and so was a clean test of whether img2img will ADD an\nobject the\
    \ init lacks. It will not, at strength 0.35 or 0.55. The founder chose\n`inpaint` over `drop` on 2026-08-10\
    \ (ledger `ep2-b01-fig-route-0810`), so the\nfruit becomes its own step and this is that step.\n\n\
    THE METHOD, AND EVERY CHOICE IN IT IS SOURCED RATHER THAN INVENTED.\n\n  * animagine-xl-3.1 has NO\
    \ inpainting variant, so this loads the BASE weights\n    into StableDiffusionXLInpaintPipeline. diffusers\
    \ 0.29.2 supports that\n    explicitly -- its own check is \"The unet should have either 4 or 9 input\n\
    \    channels\", and at 4 (a normal SDXL checkpoint) it takes the latent-blend\n    branch instead\
    \ of concatenating mask channels:\n        latents = (1 - init_mask) * init_latents_proper + init_mask\
    \ * latents\n    HF's docs name the trade-off in their own words: \"the overall image quality\n  \
    \  may be lower, but it generally tends to preserve the mask area (that is why\n    you can see the\
    \ mask outline)\". Preserving the unmasked area is exactly what\n    we want here -- the plate is\
    \ the founder's and must come back unchanged\n    outside the mask.\n  * `padding_mask_crop` is the\
    \ countermeasure for the failure mode this beat has:\n    a SMALL object in a LARGE frame. It crops\
    \ the masked region with padding,\n    upscales that crop to the pipeline resolution, inpaints there,\
    \ and pastes it\n    back -- so the fruit is drawn at full model resolution instead of at the ~5%\n\
    \    of the frame it will occupy. Verified present in 0.29.2's __call__ on the box.\n  * `strength`\
    \ must be high to ADD something. It is not the img2img case; the\n    unmasked region is restored\
    \ every step by the blend above, so a high strength\n    costs nothing outside the mask.\n\n    diffusers\
    \ 0.29.2 source: https://raw.githubusercontent.com/huggingface/diffusers/v0.29.2/src/diffusers/pipelines/stable_diffusion_xl/pipeline_stable_diffusion_xl_inpaint.py\n\
    \    inpaint docs:            https://huggingface.co/docs/diffusers/v0.29.2/en/using-diffusers/inpaint\n\
    \    model card + licence:    https://huggingface.co/cagliostrolab/animagine-xl-3.1\n    padding_mask_crop\
    \ issue: https://github.com/huggingface/diffusers/issues/6345\n\nWHAT THIS SCRIPT REFUSES TO DO. It\
    \ will not run on an init whose sha256 does not\nmatch the one named on the command line. G1 says\
    \ a conditioned render starts from\na frame the founder has passed, and the only way that gate means\
    \ anything is if\nthe bytes are asserted rather than the filename. It also never writes a canon\n\
    filename and always writes `approved: false` in the sidecar.\n\nTHE MASK IS GEOMETRY AND IT IS THE\
    \ STEWARD'S, NOT HIS. He approved a method, not\na size or a position. The ellipse is passed in on\
    \ the command line, drawn to a\nPNG beside the output so it can be looked at, and reported in the\
    \ sidecar in both\npixels and as a fraction of the frame, so the first correction he makes can be\n\
    \"lower\", \"smaller\", \"other side\" and it is one number.\n\nSECOND CALLER, 2026-08-15: THE BARK\
    \ BOARD, AND WHY THE MASK GREW STRAIGHT EDGES.\nA prop -- a clipboard made of bark -- gates three\
    \ beats of 002b (06 \"turns over a\nclipboard made of bark and reads\", 08 \"lowers the clipboard\
    \ and points\", 10\n\"flips the clipboard around: the back is blank\"). Four wording attempts closed\n\
    the wording lever for good (see `a5e61487`, `c7a11ff0`): they established that\nwording reaches the\
    \ prop's MATERIAL -- deleting the noun killed the chrome clip\nand white paper in 12 frames of 12\
    \ -- and that wording does NOT reach its\nGEOMETRY. `large flat rectangular` was in the positive of\
    \ a clean, uncontested\nprompt and all four seeds still rounded the object into a lozenge, a dome,\
    \ a\nsurfboard blank and a basket lid. So the geometry has to come from the mask, and\nan ELLIPSE\
    \ MASK CANNOT SUPPLY IT: an ellipse is the rounded blob that four\nprompts already failed against,\
    \ and masking a board with one would re-draw the\ndefect under test. Hence `--quad`, four corners\
    \ and four straight edges. A\nrectangle is a quad; a rectangular board held at an angle is a quad\
    \ in\nperspective, which an axis-aligned rectangle would not fit either.\n\nTHIRD CALLER, 2026-08-17:\
    \ EYEWEAR ON ONE MAN OF TWO, AND WHY THE MASK BECAME\nARBITRARY. `glasses` named in ONE man's clause\
    \ lands on BOTH men, 5 of 5 -- a\ndifferent failure from the prop above, which goes to ONE WRONG figure.\
    \ The\nfounder ruled \"draw the second man without glasses. we need to have control.\"\nThe published\
    \ cause (ALE-Edit, arXiv 2412.04715) is that CLIP is causal and its\nEOS carries global prompt semantics,\
    \ so the token steers the whole frame and no\nwording says WHICH face; a negative cannot unbind it.\
    \ The mask is therefore the\nonly lever, and the region that matters here is a THIN BAND along a spectacle\n\
    frame -- neither a blob nor a convex polygon, so neither `--ellipse` nor `--quad`\ncan express it.\
    \ Hence `--mask-png`, which also retires the board-MINUS-the-hand\nlimitation recorded against this\
    \ script in wave-drafts.yaml. The band is authored\nby pipeline/attribute_mask.py, a $0 step with\
    \ no GPU and no sampler in it, which\nis what makes \"control\" checkable instead of asserted.\n\n\
    \    python inpaint_fruit.py --init PLATE.png --init-sha256 <hex> \\\n        (--ellipse cx,cy,rx,ry\
    \ | --quad x0,…,y3 | --mask-png m.png) \\\n        --prompt-file p.txt --negative-file n.txt \\\n\
    \        --out OUT.png [--steps 40] [--cfg 7.5] [--strength 0.99] \\\n        [--seed N] [--pad-crop\
    \ 64] [--blur 8] [--dry-run]\n\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport\
    \ datetime as _dt\nimport hashlib\nimport os\nimport sys\nimport time\n\nBASE = \"cagliostrolab/animagine-xl-3.1\"\
    \nBASE_LICENCE = \"CreativeML Open RAIL++-M (use restrictions travel; D15)\"\n\n\ndef sha256_of(path:\
    \ str) -> str:\n    h = hashlib.sha256()\n    with open(path, \"rb\") as fh:\n        for chunk in\
    \ iter(lambda: fh.read(1 << 20), b\"\"):\n            h.update(chunk)\n    return h.hexdigest()\n\n\
    \ndef read_text(path: str) -> str:\n    with open(path, \"r\", encoding=\"utf-8\") as fh:\n      \
    \  return fh.read().strip()\n\n\ndef yaml_block(text: str, indent: str = \"  \") -> str:\n    return\
    \ \"\\n\".join(indent + line for line in text.splitlines())\n\n\ndef module_version(name: str) ->\
    \ str:\n    try:\n        return __import__(name).__version__\n    except Exception:\n        return\
    \ \"unresolved\"\n\n\ndef main() -> int:\n    ap = argparse.ArgumentParser(description=__doc__,\n\
    \                                 formatter_class=argparse.RawDescriptionHelpFormatter)\n    ap.add_argument(\"\
    --init\", required=True)\n    ap.add_argument(\"--init-sha256\", required=True,\n                \
    \    help=\"asserted before anything loads; a mismatch is a hard stop\")\n    ap.add_argument(\"--ellipse\"\
    , default=\"\",\n                    help=\"cx,cy,rx,ry in pixels of the init image\")\n    ap.add_argument(\"\
    --quad\", default=\"\",\n                    help=\"x0,y0,x1,y1,x2,y2,x3,y3 -- four corners in order,\
    \ a \"\n                         \"STRAIGHT-EDGED mask. A rectangle is a quad, and a \"\n        \
    \                 \"rectangular board seen at an angle is a quad and is NOT \"\n                 \
    \        \"an ellipse. Exactly one of --ellipse/--quad/--mask-png \"\n                         \"\
    is required.\")\n    ap.add_argument(\"--mask-png\", default=\"\",\n                    help=\"an\
    \ 8-bit mask PNG the same size as --init: white is \"\n                         \"redrawn, black is\
    \ preserved. This is the general case \"\n                         \"the two shape flags are special\
    \ cases of. It exists \"\n                         \"because a region worth masking is often neither\
    \ a blob \"\n                         \"nor a convex polygon -- a THIN BAND along a spectacle \"\n\
    \                         \"frame, or a board MINUS the hand holding it (a \"\n                  \
    \       \"limitation recorded against this script in \"\n                         \"wave-drafts.yaml\
    \ and not expressible as one ellipse or \"\n                         \"one quad). Authoring the geometry\
    \ is a separate $0 step \"\n                         \"with no GPU and no sampler in it, which is\
    \ the point: \"\n                         \"WHICH PIXELS MAY CHANGE BECOMES A DECISION WE MAKE AND\
    \ \"\n                         \"CAN DIFF, not something the model gets a vote on. See \"\n      \
    \                   \"pipeline/attribute_mask.py.\")\n    ap.add_argument(\"--dry-run\", action=\"\
    store_true\",\n                    help=\"assert the sha, draw the mask PNG beside --out and stop\
    \ \"\n                         \"BEFORE loading any model. Costs nothing and is the step \"\n    \
    \                     \"where a misplaced mask gets caught by eye.\")\n    ap.add_argument(\"--prompt-file\"\
    , required=True)\n    ap.add_argument(\"--negative-file\", required=True)\n    ap.add_argument(\"\
    --out\", required=True)\n    ap.add_argument(\"--steps\", type=int, default=40)\n    ap.add_argument(\"\
    --cfg\", type=float, default=7.5)\n    ap.add_argument(\"--strength\", type=float, default=0.99)\n\
    \    ap.add_argument(\"--seed\", type=int, default=20260810)\n    ap.add_argument(\"--pad-crop\",\
    \ type=int, default=64,\n                    help=\"padding_mask_crop; 0 disables it\")\n    ap.add_argument(\"\
    --blur\", type=int, default=8, help=\"mask blur_factor\")\n    ap.add_argument(\"--note\", default=\"\
    \")\n    a = ap.parse_args()\n\n    if not os.path.isfile(a.init):\n        print(\"!! init not found:\
    \ %s\" % a.init, flush=True)\n        return 2\n    have = sha256_of(a.init)\n    if have != a.init_sha256:\n\
    \        print(\"!! INIT SHA MISMATCH -- refusing.\\n   want %s\\n   have %s\"\n              % (a.init_sha256,\
    \ have), flush=True)\n        return 3\n\n    if sum(1 for v in (a.ellipse, a.quad, a.mask_png) if\
    \ v) != 1:\n        print(\"!! pass exactly one of --ellipse, --quad or --mask-png\", flush=True)\n\
    \        return 2\n\n    cx = cy = rx = ry = 0\n    corners: list = []\n    mask_sha = \"\"\n    if\
    \ a.mask_png:\n        if not os.path.isfile(a.mask_png):\n            print(\"!! --mask-png not found:\
    \ %s\" % a.mask_png, flush=True)\n            return 2\n        mask_sha = sha256_of(a.mask_png)\n\
    \        shape = \"png\"\n        bbox = []\n    elif a.ellipse:\n        try:\n            cx, cy,\
    \ rx, ry = (int(v) for v in a.ellipse.split(\",\"))\n        except Exception:\n            print(\"\
    !! --ellipse wants cx,cy,rx,ry\", flush=True)\n            return 2\n        shape = \"ellipse\"\n\
    \        bbox = [cx - rx, cy - ry, cx + rx, cy + ry]\n    else:\n        try:\n            vals =\
    \ [int(v) for v in a.quad.split(\",\")]\n        except Exception:\n            vals = []\n      \
    \  if len(vals) != 8:\n            print(\"!! --quad wants x0,y0,x1,y1,x2,y2,x3,y3 (8 integers)\"\
    , flush=True)\n            return 2\n        corners = list(zip(vals[0::2], vals[1::2]))\n       \
    \ xs = [p[0] for p in corners]\n        ys = [p[1] for p in corners]\n        shape = \"quad\"\n \
    \       bbox = [min(xs), min(ys), max(xs), max(ys)]\n\n    from PIL import Image, ImageDraw\n\n  \
    \  plate = Image.open(a.init).convert(\"RGB\")\n    W, H = plate.size\n    mask = Image.new(\"L\"\
    , (W, H), 0)\n    if shape == \"png\":\n        supplied = Image.open(a.mask_png).convert(\"L\")\n\
    \        if supplied.size != (W, H):\n            print(\"!! MASK SIZE MISMATCH -- refusing.\\n  \
    \ init %dx%d\\n   mask %dx%d\"\n                  % (W, H, supplied.size[0], supplied.size[1]), flush=True)\n\
    \            return 6\n        mask = supplied\n        box = mask.point(lambda v: 255 if v > 0 else\
    \ 0).getbbox()\n        if box is None:\n            print(\"!! mask is entirely black -- nothing\
    \ would be redrawn.\", flush=True)\n            return 7\n        # getbbox()'s right/bottom are exclusive;\
    \ the sidecar reports inclusive px.\n        bbox = [box[0], box[1], box[2] - 1, box[3] - 1]\n   \
    \ elif shape == \"ellipse\":\n        ImageDraw.Draw(mask).ellipse(bbox, fill=255)\n    else:\n  \
    \      ImageDraw.Draw(mask).polygon(corners, fill=255)\n\n    os.makedirs(os.path.dirname(os.path.abspath(a.out))\
    \ or \".\", exist_ok=True)\n    mask_png = os.path.splitext(a.out)[0] + \"-mask.png\"\n    mask.save(mask_png)\n\
    \n    prompt = read_text(a.prompt_file)\n    negative = read_text(a.negative_file)\n\n    if a.dry_run:\n\
    \        print(\"DRY RUN -- no model loaded, nothing rendered.\", flush=True)\n        print(\"init_sha256\
    \ OK %s\" % have, flush=True)\n        print(\"mask %s corners=%s bbox=%s  %.4f x %.4f of %dx%d\"\n\
    \              % (shape, corners or \"-\", bbox,\n                 (bbox[2] - bbox[0]) / float(W),\n\
    \                 (bbox[3] - bbox[1]) / float(H), W, H), flush=True)\n        print(\"WROTE %s\" %\
    \ mask_png, flush=True)\n        print(\"rc=0 dry_run=1\", flush=True)\n        return 0\n\n    import\
    \ torch\n    from diffusers import StableDiffusionXLInpaintPipeline\n\n    if not torch.cuda.is_available():\n\
    \        print(\"!! no CUDA. This is the box's job; stopping.\", flush=True)\n        return 4\n\n\
    \    t0 = time.time()\n    pipe = StableDiffusionXLInpaintPipeline.from_pretrained(\n        BASE,\
    \ torch_dtype=torch.bfloat16, use_safetensors=True)\n    pipe = pipe.to(\"cuda\")\n    pipe.set_progress_bar_config(disable=True)\n\
    \    in_ch = int(pipe.unet.config.in_channels)\n    print(\"MODEL_LOADED %s cuda/bfloat16 unet.in_channels=%d\
    \ in %.0fs\"\n          % (BASE, in_ch, time.time() - t0), flush=True)\n    if in_ch not in (4, 9):\n\
    \        print(\"!! unexpected unet.in_channels=%d\" % in_ch, flush=True)\n        return 5\n\n  \
    \  blurred = pipe.mask_processor.blur(mask, blur_factor=a.blur) if a.blur else mask\n\n    kwargs\
    \ = dict(prompt=prompt, negative_prompt=negative, image=plate,\n                  mask_image=blurred,\
    \ width=W, height=H,\n                  num_inference_steps=a.steps, guidance_scale=a.cfg,\n     \
    \             strength=a.strength,\n                  generator=torch.Generator(\"cuda\").manual_seed(a.seed))\n\
    \    if a.pad_crop:\n        kwargs[\"padding_mask_crop\"] = a.pad_crop\n\n    t1 = time.time()\n\
    \    out = pipe(**kwargs).images[0]\n    render_s = time.time() - t1\n\n    out.save(a.out)\n\n  \
    \  versions = {\"python\": sys.version.split()[0], \"torch\": module_version(\"torch\"),\n       \
    \         \"diffusers\": module_version(\"diffusers\")}\n    stamp = _dt.datetime.now(_dt.timezone.utc).strftime(\"\
    %Y-%m-%dT%H:%M:%SZ\")\n    mask_lines = [\"mask_shape: %s\" % shape]\n    if shape == \"png\":\n \
    \       mask_lines += [\"mask_png_source: %s\" % a.mask_png.replace(\"\\\\\", \"/\"),\n          \
    \             \"mask_png_sha256: %s\" % mask_sha,\n                       \"mask_white_px: %d\"\n\
    \                       % (W * H - mask.point(lambda v: 255 if v > 0 else 0)\n                   \
    \       .histogram()[0])]\n    elif shape == \"ellipse\":\n        mask_lines += [\"mask_centre_px:\
    \ [%d, %d]\" % (cx, cy),\n                       \"mask_radii_px: [%d, %d]\" % (rx, ry)]\n    else:\n\
    \        mask_lines += [\"mask_corners_px: %s\"\n                       % str([[int(x), int(y)] for\
    \ x, y in corners])]\n    mask_lines += [\n        \"mask_bbox_px: [%d, %d, %d, %d]\" % tuple(bbox),\n\
    \        \"mask_width_frac: %.4f\" % ((bbox[2] - bbox[0]) / float(W)),\n        \"mask_height_frac:\
    \ %.4f\" % ((bbox[3] - bbox[1]) / float(H)),\n    ]\n    sidecar = a.out + \".meta.yaml\"\n    with\
    \ open(sidecar, \"w\", encoding=\"utf-8\", newline=\"\\n\") as fh:\n        fh.write(\"\\n\".join([\n\
    \            \"# Still provenance (7.2), written AT RENDER TIME by inpaint_fruit.py\",\n         \
    \   \"# on the rtx5090. The plate outside the mask is the founder's own\",\n            \"# pixels;\
    \ only the region described below was redrawn.\",\n            \"platform: local-gpu (rtx5090)\",\n\
    \            \"model: %s\" % BASE,\n            \"model_licence: %s\" % BASE_LICENCE,\n          \
    \  \"pipeline: StableDiffusionXLInpaintPipeline (base weights, unet.in_channels=%d)\" % in_ch,\n \
    \           \"size: %dx%d\" % (W, H),\n            \"steps: %d\" % a.steps,\n            \"guidance:\
    \ %s\" % a.cfg,\n            \"strength: %s\" % a.strength,\n            \"seed: %d\" % a.seed,\n\
    \            \"padding_mask_crop: %s\" % (a.pad_crop or \"null\"),\n            \"mask_blur_factor:\
    \ %d\" % a.blur,\n            \"init_image: %s\" % a.init.replace(\"\\\\\", \"/\"),\n            \"\
    init_sha256: %s\" % have,\n            \"mask_png: %s\" % os.path.basename(mask_png),\n        ] +\
    \ mask_lines + [\n            \"mask_is_the_stewards: >-\",\n            yaml_block(\"THE FOUNDER\
    \ APPROVED A METHOD, NOT THIS GEOMETRY. He said \"\n                       \"`inpaint` and named no\
    \ size, no position, no colour and no \"\n                       \"shape. The mask above is the steward's\
    \ and is the first \"\n                       \"thing his correction should move -- 'lower', 'smaller',\
    \ \"\n                       \"'other side' is one number here; the words in the prompt \"\n     \
    \                  \"are his own approved shots.md wording.\"),\n            \"rendered_utc: %s\"\
    \ % stamp,\n            \"render_seconds: %.1f\" % render_s,\n            \"wall_seconds: %.1f\" %\
    \ (time.time() - t0),\n            \"cost_usd: 0\",\n            \"python_version: %s\" % versions[\"\
    python\"],\n            \"torch_version: %s\" % versions[\"torch\"],\n            \"diffusers_version:\
    \ %s\" % versions[\"diffusers\"],\n            \"approved: false\",\n            \"provisional: >-\"\
    ,\n            yaml_block(\"PROVISIONAL. A steward-rendered SAMPLE, not a pick and not \"\n      \
    \                 \"canon. Never takes a canon filename, is not published, not \"\n              \
    \         \"posted, and not assembled into an episode. Ground truth is \"\n                      \
    \ \"the founder (R4).\"),\n            \"note: >-\",\n            yaml_block(a.note or \"one inpainted\
    \ sample; the fruit question.\"),\n            \"prompt: |-\",\n            yaml_block(prompt),\n\
    \            \"negative: |-\",\n            yaml_block(negative),\n            \"\",\n        ]))\n\
    \n    print(\"WROTE %s\" % a.out, flush=True)\n    print(\"WROTE %s\" % mask_png, flush=True)\n  \
    \  print(\"WROTE %s\" % sidecar, flush=True)\n    print(\"rc=0 render_s=%.1f\" % render_s, flush=True)\n\
    \    return 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n"
  C:\banyan-farm\b08gesture-0818\prompt.txt: a tall bald guard in a brown cloak lowers his clipboard and
    points one finger at the green goblin's belly beside him, both men standing together on the same grass
    field, detailed cinematic anime, masterpiece, best quality, very aesthetic
  C:\banyan-farm\b08gesture-0818\negative.txt: giant, colossus, towering figure, huge, different scale,
    floating, deleted sky, raised clipboard, second board, extra board, extra arms, extra hands, extra
    fingers, deformed hands, third person, crowd, text, watermark, photorealism, 3d render, low quality,
    deformed
steps:
- name: fetch
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\b08gesture-0818\fetch_init.py
- name: dry
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\b08gesture-0818\inpaint_fruit.py
  - --init
  - C:\banyan-farm\b08gesture-0818\08-inside-him-twofig-mac-r3-s0.png
  - --init-sha256
  - d9cd8b1a4642a60a3362d07feef30b47999bbe988e4ef52d047d1f4d06113238
  - --ellipse
  - 445,620,335,165
  - --prompt-file
  - C:\banyan-farm\b08gesture-0818\prompt.txt
  - --negative-file
  - C:\banyan-farm\b08gesture-0818\negative.txt
  - --out
  - C:\banyan-farm\b08gesture-0818\b08-twofig-gesture-DRY.png
  - --steps
  - '40'
  - --cfg
  - '7.5'
  - --strength
  - '0.30'
  - --pad-crop
  - '64'
  - --blur
  - '8'
  - --seed
  - '20260830'
  - --note
  - 'mask geometry check. Writes the mask and exits before a model is loaded. The ellipse was measured
    off the init on 2026-08-18 and looked at: x 110-780, y 455-785 of 832x1216, which covers the guard''s
    two hands and the whole clipboard, the gap between the men, the goblin''s outstretched hand and his
    belly and sash -- and clears BOTH FACES and BOTH LOWER ROBES, so neither identity nor the ground plane
    is inside the region being redrawn.'
  - --dry-run
- name: s20260830
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\b08gesture-0818\inpaint_fruit.py
  - --init
  - C:\banyan-farm\b08gesture-0818\08-inside-him-twofig-mac-r3-s0.png
  - --init-sha256
  - d9cd8b1a4642a60a3362d07feef30b47999bbe988e4ef52d047d1f4d06113238
  - --ellipse
  - 445,620,335,165
  - --prompt-file
  - C:\banyan-farm\b08gesture-0818\prompt.txt
  - --negative-file
  - C:\banyan-farm\b08gesture-0818\negative.txt
  - --out
  - C:\banyan-farm\b08gesture-0818\b08-twofig-gesture-s20260830.png
  - --steps
  - '40'
  - --cfg
  - '7.5'
  - --strength
  - '0.30'
  - --pad-crop
  - '64'
  - --blur
  - '8'
  - --seed
  - '20260830'
  - --note
  - 'ONE SAMPLE, ONE SEED, ONE VARIABLE (the gesture). The composite/init route ruled for beat 08 on 2026-08-18:
    the pair, their adult scale relative to each other and the single ground plane all come from the init
    and none of them from the words, because the words are the thing that was falsified -- every wording
    round returned a colossus, and the round that deleted the sky returned one anyway. Only the gesture
    is asked of the sampler. Strength 0.30 is the proven finish band (8 of 8 exact counts on beat 01);
    see this spec''s header for why a FAIL here is expected to be ambiguous between ''the route cannot
    do it'' and ''0.30 finishes and does not add''.'
- name: publish
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - "# EVERY FILE IS NAMED IN FULL, not matched by a glob. A glob that misses\n# publishes nothing while\
    \ returning a code that reads like a render failure,\n# and box_enqueue.output_path_problems refuses\
    \ a spec whose declared artifacts\n# are never named by any step -- which a wildcard defeats. Missing\
    \ ONE file is\n# a FAIL of the job. No allow_fail anywhere in this spec.\nimport hashlib, os, shutil\n\
    from_dir = \"C:/banyan-farm/b08gesture-0818\"\ndst = \"C:/banyan-farm/courier-box/farm-out/ep2-b08-twofig-gesture-0818\"\
    \nNAMES = [\n    \"b08-twofig-gesture-s20260830.png\",\n    \"b08-twofig-gesture-s20260830-mask.png\"\
    ,\n    \"b08-twofig-gesture-s20260830.png.meta.yaml\",\n    \"08-inside-him-twofig-mac-r3-s0.png\"\
    ,\n    \"prompt.txt\",\n    \"negative.txt\",\n]\nos.makedirs(dst, exist_ok=True)\nlines = []\nfor\
    \ name in NAMES:\n    f = os.path.join(from_dir, name)\n    if not os.path.isfile(f):\n        raise\
    \ SystemExit(\"!! missing %s -- refusing to call the job clean.\" % f)\n    shutil.copy2(f, dst)\n\
    \    with open(os.path.join(dst, name), \"rb\") as fh:\n        h = hashlib.sha256(fh.read()).hexdigest()\n\
    \    lines.append(h + \"  \" + name)\nwith open(os.path.join(dst, \"ep2-b08-twofig-gesture-0818.sha256\"\
    ), \"w\",\n          newline=\"\\n\") as fh:\n    fh.write(\"\\n\".join(sorted(lines)) + \"\\n\")\n\
    print(\"published\", len(lines), \"file(s) + manifest ->\", dst)\nraise SystemExit(0 if len(lines)\
    \ == len(NAMES) else 1)\n"
artifacts:
- C:\banyan-farm\b08gesture-0818\b08-twofig-gesture-s20260830.png
- C:\banyan-farm\b08gesture-0818\08-inside-him-twofig-mac-r3-s0.png
script_authority: 'Node 002b-first-citizen, `approved_by: founder`, `approved_on: 2026-08-03`, so §6 is
  satisfied. The prompt animates beat 08''s own scripted action -- node.md 0:37-0:42, "Guard 2 lowers
  the clipboard and points at the scavenger''s belly" -- and nothing else. This is a STILL, not a render
  of motion, not a voice synthesis and not an episode assembly. inpaint_fruit.py writes `approved: false`
  and a `provisional:` block into the sidecar itself at render time, so the sample says on its face that
  it is not a pick and not canon; the taste call is the founder''s under R4. shots.md and pipeline/wave-drafts.yaml
  are UNTOUCHED.'

# ============================================================================
# OUTCOME, appended 2026-08-18 after the render. NOTHING ABOVE THIS LINE HAS
# BEEN EDITED -- the bar it is judged against is the one committed BEFORE the
# card ran, and it has not been rewritten to fit the result. The authoring
# script pipeline/author_b08_gesture_0818.py does NOT reproduce this block; it
# regenerates everything above it, so re-running it discards the outcome.
# ============================================================================
outcome:
  verdict: FAIL, on B4 only, and it is exactly the outcome this spec predicted (G1).
  ran: 'rtx5090, 2026-08-18T15:30:35Z -> 15:30:49Z, all four steps rc=0. Model load 6 s, render 3.8 s,
    14 s wall. $0. init_sha256 asserted OK by both the fetch step and inpaint_fruit.py before any weight
    loaded: d9cd8b1a4642a60a3362d07feef30b47999bbe988e4ef52d047d1f4d06113238. Mask bbox [110, 455, 780,
    785], 0.8053 x 0.2714 of 832x1216, i.e. the geometry this spec wrote down and looked at.'
  b1_one_ground_plane: PASS. Both men stand on the same grass, feet at the same depth. Untouched by the render.
  b2_guard_adult: PASS. The bald human in the brown cloak reads adult.
  b3_goblin_adult_identity: PASS. Same green adult goblin, teal robe, pointed ears, same face. Not a child,
    not a chibi, not a second character.
  b4_clipboard_down_and_point: 'FAIL. THE CLIPBOARD IS IN THE SAME RAISED POSITION IT HOLDS IN THE INIT,
    gripped in both hands at chest height, and nobody points at anything. Read off an A/B of init against
    output at full size: the board''s two top corners, its clip and both of the guard''s hands are where
    they were.'
  b5_no_colossus: PASS. Neither figure towers over the other; the scale relationship the init carries survived
    intact, which is the clause the wording lever could never hold.
  fail_modes_hit: G1 only. NOT G2 (no second board, no third arm, no extra hand), NOT G3, NOT G4, NOT G5.
  what_actually_changed: 'MEASURED, not eyeballed, because "nothing happened" and "the region was never
    touched" are different findings and only one of them is true here. Max-channel absolute difference,
    init against output: INSIDE the mask mean 12.86 with 32.9% of pixels moving more than 8 levels; OUTSIDE
    the mask mean 0.06 with 0.1% moving more than 8 levels. So the sampler DID redraw a third of the masked
    region -- the goblin''s soft dark sash became a hard-edged maroon belt, his hanging board gained a
    cleaner outline and a different clip, his outstretched fingers were re-inked, the guard''s sleeve folds
    were redrawn -- AND IT SPENT ALL OF THAT REDRAWING THE SAME STAGING. That is what "finish, do not
    add" looks like when you ask it to add.'
  what_this_settles: 'THE INIT HALF OF THE RULED ROUTE WORKS AND THE WORDING HALF STILL DOES NOT. Four
    of the five bar clauses -- the pair, both adult reads, one ground plane, no colossus -- came through
    a 12-step denoise completely unharmed, which is precisely what the composite/init route was ruled in
    to buy after every wording round returned a colossus. What it does not buy is the gesture.'
  what_this_does_not_settle: 'STATED RATHER THAN GLOSSED, and this spec said so before the render: ONE
    SAMPLE CANNOT SEPARATE "the composite/init route cannot reach beat 08''s staging" from "0.30 finishes
    and does not add". This result is consistent with both and must not be quoted as though it closed the
    route. What it DOES do is make the next step concrete: the lowered board and the pointing arm are a
    STRUCTURAL change, so the next honest lever is a structural composite of them -- the same shape as
    beat 01''s leaf count, where the count came from plain image processing and the sampler only made it
    look drawn -- and NOT a higher strength. composite-init-pattern.md §2 forbids the higher-strength rescue
    by name, and the beat-08 §9 record already has 0.45 failing on this beat''s neighbours by inventing.'
  a_side_finding_worth_keeping: 'THIS PATH IS FIGURE-PRESERVING AND THE MPS PATH IS NOT. farm-out/ep2-b14-fieldcomp-0817''s
    sidecar records that beat14_field_inpaint.py (MPS, blur 8, NO padding_mask_crop) changed 16.1% of its
    protected region by more than 8 levels and warns that "outside the mask is the founder''s own pixels"
    is FALSE on that path. On THIS path -- inpaint_fruit.py on CUDA with padding_mask_crop 64 -- the number
    is 0.1%. The claim is true here. The difference is pad-crop, and any lane choosing between the two
    scripts should choose on that.'
  not_done_on_purpose: 'ONE SAMPLE, ONE SEED. No sweep, no second strength, no wording variant, no re-mask.
    A higher strength is not tried as a rescue, by the pattern''s own stop rule. The picture is `approved:
    false` and `provisional:` on its own sidecar, written by the tool at render time; it is not a pick,
    not canon and not cut. R4 is the founder''s.'
