# EPISODE 2, BEAT 20 EVIDENCE - r2 OF THE IDENTITY-FROZEN ROUND. One sample,
# four seeds, $0. 2026-08-12, the fixer lane. THE CLEANEST TEST OF THE MECHANISM.
#
# WHAT ALREADY LANDS AND MUST NOT REGRESS. This is the best goblin beat of the
# wave on content: both frames read give him a ripe fig held in both hands in
# front of him, which is exactly the beat, and s0 puts the BARE BRANCH above him,
# which is the other half of the shot. Green skin, bald, patchwork cloak and the
# sunny field are all present. The positive is therefore not touched at all.
#
# THE FAULT: a large pale cream dome over a green face in both frames read - but
# NO SUTURES. That makes beat 20 the clean test of the other direction. Beat 02
# showed the ban removes the dome and the stitches TOGETHER; this beat has the
# dome WITHOUT the stitches. If the ban clears it here, "one fault, not two" is
# confirmed from both sides and the whole wave's scalp story is settled. If it
# does not, the dome is its own fault and the answer is the `green scalp`
# affirmative that beat 03 is testing separately - which is exactly why that
# affirmative was kept out of these propagation rounds.
#
# THE LEVER IS PROVEN ON THIS CHECKPOINT TONIGHT, so this is a PROPAGATION of a
# measured one-variable result rather than a new guess. `ep2-b02-idfix-r2-0812`
# added `no scars, no stitched skin` and nothing else in that channel. Its four
# frames came back with unbroken GREEN crowns 4/4 - no graft, no suture, no seam
# - and the pale cream dome went WITH the stitches, which means the tan patch and
# the sutures were ONE fault, fabric on the skull, not two. The patchwork cloak
# survived 4/4, s2 still showing stitched seams ON THE CLOAK, so naming SKIN
# provably does not negate what the positive asserts.
#
# THE SCALP AFFIRMATIVE IS DELIBERATELY NOT STACKED ON. `green scalp` is under
# test separately on beat 03; beat 02 showed the ban alone clears the dome.
#
# RECORDED, NOT FIXED: the skull is inflated toward chibi, s2 worst.
#
# BOX DRY-RUN: positive 73/77, anchor present, count tag '1boy', negative 77 ->
# sent 77 with `scars` and `stitched skin` sent. POSITIVE BYTE-IDENTICAL.
#
# SEPARATE OUT DIRECTORY. Writes to `out-b20-idfix-r2`; overwrites no evidence.

id: ep2-b20-idfix-r2-0812
task: ep2-b20-idfix-r2-0812
node: 002b-first-citizen
beat: 20
runner: box
needs_gpu: true

priority: 44
max_attempts: 1
owner: overnight fixer lane, 2026-08-12 (reading the identity-frozen wave and firing one r2 per faulty beat)

# NO `gate:` KEY. One sample moving one channel on a recipe he has ratified.

script_authority: 'Node 002b-first-citizen, live script `002b-t0-c`,
  `approved_by: founder`, `approved_on: 2026-08-03` (read from
  genomes/sapling/nodes/002b-first-citizen/leaves/002b-t0-c.yaml:17-18, the leaf the
  render gate itself reads). His `approved_quote` verbatim: "approve 002b except.. we
  need to make SURE the fig growing on the sapling is mentioned, last time we generated
  002b it just.. appeared. also, the sapling was able to flail a leaf in episode 1? why
  couldnt it do that in 002b?" Both `approval_conditions` are about beat 01''s fig and
  beat 16''s leaf; neither touches beat 20.'

consumer: >-
  Roman's next look at beat 20, as a PAIR against the `out-b20-idfix` frames - same four seeds, one channel apart. It ranks nothing and picks nothing; the taste call stays R4's.

success: >-
  Four PNGs at C:\banyan-farm\wave-goblin-prep\out-b20-idfix-r2\20-evidence-wave1-s0..3.png with their
  .yaml provenance sidecars written at render time (7.2), all eight declared as
  artifacts so a missing one fails rc=92 instead of passing quietly. Whether the
  frames are good is R4's call, not this job's and not a metric's.

why: >-
  One variable against `ep2-b20-idfix-0812`: `no scars, no stitched skin`. This beat carries the pale dome WITHOUT sutures, so it tests whether the ban that removed both together on beat 02 also removes the dome alone. Positive byte-identical.

goblin_def: green skin, bald head, patchwork cloak
goblin_def_source: >-
  The frozen identity, ledger `ep2-frozen-identities-0812`, passed byte-identical to the `_idfix` round this answers. NOT touched - the fault is the model spreading `patchwork` onto skin, and the fix is in the beat's negative, not the definition. That the definition is the underlying CAUSE is argued separately, from the beat-08 control, in review/ep2-picks/wave2-needs-founder.md.

est_minutes: 2
needs:
- cuda
- vram20
- farm-venv

env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True

# The box's wave-drafts.yaml was scp'd from this repo and `findstr`-verified to
# carry `authored_b20_idfix_r2` before this job was queued, and the recipe below was
# dry-run on the box's real CLIP tokenizer. THE CHECK IS THE KEY, NOT THE
# WHOLE-FILE SHA: sibling lanes append their own variants tonight and every append
# moves the file sha without touching this variant. The harness REFUSES rather than
# falling back when a key is missing, so a stale copy fails for $0.
steps:
- name: measure
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '20'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b20_idfix_r2
  - --dry
- name: sample
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '20'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b20_idfix_r2
  - --out
  - C:\banyan-farm\wave-goblin-prep\out-b20-idfix-r2
  - --task
  - ep2-b20-idfix-r2-0812

# DURABILITY: every still this farm makes lands single-copy on a box nothing backs
# up. The courier runs `git add -A -- farm-out` and pushes on job_done, and
# farm-out is NOT gitignored, so publishing is a COPY and the runner stays
# byte-identical. allow_fail: a failed copy must never mark a good render failed.
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - |
    import glob, hashlib, os, shutil
    dst = "C:/banyan-farm/courier-box/farm-out/ep2-b20-idfix-r2"
    os.makedirs(dst, exist_ok=True)
    src = sorted(glob.glob("C:/banyan-farm/wave-goblin-prep/out-b20-idfix-r2/20-evidence-wave1-s*.*"))
    lines = []
    for f in src:
        shutil.copy2(f, dst)
        # Hash the COPY, not the original: what this records has to be the bytes
        # that get committed, or the manifest attests to the wrong file.
        c = os.path.join(dst, os.path.basename(f))
        with open(c, "rb") as fh:
            h = hashlib.sha256(fh.read()).hexdigest()
        lines.append(h + "  " + os.path.basename(f))
        print("   ", os.path.basename(f), h[:16])
    # newline="\n" is load-bearing: Windows text mode rewrites \n to \r\n and every
    # filename then carries a trailing \r that shasum -c cannot open.
    with open(os.path.join(dst, "20-evidence-idfix-r2.sha256"), "w", newline="\n") as fh:
        fh.write("\n".join(lines) + "\n")
    print("published", len(src), "file(s) + manifest ->", dst)
    # 4 stills + 4 sidecars. Fewer means the render wrote less than it promised.
    raise SystemExit(0 if len(src) == 8 else 1)

artifacts:
- C:\banyan-farm\wave-goblin-prep\out-b20-idfix-r2\20-evidence-wave1-s0.png
- C:\banyan-farm\wave-goblin-prep\out-b20-idfix-r2\20-evidence-wave1-s1.png
- C:\banyan-farm\wave-goblin-prep\out-b20-idfix-r2\20-evidence-wave1-s2.png
- C:\banyan-farm\wave-goblin-prep\out-b20-idfix-r2\20-evidence-wave1-s3.png
- C:\banyan-farm\wave-goblin-prep\out-b20-idfix-r2\20-evidence-wave1-s0.yaml
- C:\banyan-farm\wave-goblin-prep\out-b20-idfix-r2\20-evidence-wave1-s1.yaml
- C:\banyan-farm\wave-goblin-prep\out-b20-idfix-r2\20-evidence-wave1-s2.yaml
- C:\banyan-farm\wave-goblin-prep\out-b20-idfix-r2\20-evidence-wave1-s3.yaml
