# EPISODE 2, BEAT 13 THE SHADE - r2 OF THE IDENTITY-FROZEN ROUND. One sample,
# four seeds, $0. 2026-08-12, the fixer lane.
#
# THE FAULT: the cranium pales while the face stays green. s1 is the worst single
# case in the whole wave - nearly the entire skull cream-white above a green face
# - with a large pale dome in s2 and a moderate one in s0.
#
# THE LEVER IS PROVEN ON THIS CHECKPOINT, TONIGHT, AND THIS IS A PROPAGATION
# RATHER THAN A NEW GUESS. `ep2-b02-idfix-r2-0812` added `no scars, no stitched
# skin` and nothing else in that channel. Its four frames (farm-out/
# ep2-b02-idfix-r2/, read frame by frame) came back with unbroken GREEN crowns
# 4/4 - no graft, no suture line, no seam - and the pale cream dome went WITH the
# stitches, which means the tan patch and the sutures were ONE fault, fabric on
# the skull, and not two. The patchwork cloak survived 4/4, s2 still showing
# stitched seams ON THE CLOAK, so naming SKIN provably does not negate what the
# positive asserts.
#
# THE SCALP AFFIRMATIVE IS DELIBERATELY NOT STACKED ON. `green scalp` is under
# test separately on beat 03. Beat 02 showed the ban alone clears the dome, so
# adding the affirmative here would make the result unattributable for no gain.
#
# IT COSTS EXACTLY WHAT WAS SPARE. This beat sent 73 of 77; the two terms are four
# tokens and four were free. The box dry-run confirms the whole plant-fusion tier,
# `female goblin` and `elf` all survive, and the only casualty anywhere is
# `shrubbery` from the tail of the anti-big-tree tier - `mature tree`, `large
# tree`, `thick trunk`, `full canopy` and `forest` all still ship.
# THE POSITIVE IS BYTE-IDENTICAL at 68 tokens, so this round is one variable.
#
# RECORDED AND DELIBERATELY NOT FIXED. Three of four frames are murky, and this
# beat's negative carries only `night` and `dusk` - it has NONE of the eight-term
# dark block every other beat in the wave sends. But this beat's own subject is
# "the small patch of shade", so some of that dimness is the script rather than a
# fault, and four spare tokens cannot buy both. The proven ban has the better
# claim. If the skulls come back clean and the frames are still murky, the dark
# block is the next purchase, and this is where that was called in advance.
# ALSO LOGGED, NOT CHASED: the two-leaf seedling appears in s3 only (1/4) - the
# sapling-adjacency limit, which is a founder-facing item now.
#
# BOX DRY-RUN, THE SHIPPED RECIPE:
#   positive 68/77   STYLE ANCHOR PRESENT: True   count tag '1boy'
#   negative recipe 77 -> sent 74; `scars` and `stitched skin` both sent.
#
# SEPARATE OUT DIRECTORY. Writes to `out-b13-idfix-r2`; overwrites no evidence.

id: ep2-b13-idfix-r2-0812
task: ep2-b13-idfix-r2-0812
node: 002b-first-citizen
beat: 13
runner: box
needs_gpu: true

priority: 44
max_attempts: 1
owner: overnight fixer lane, 2026-08-12 (reading the identity-frozen wave and firing one r2 per faulty beat)

# NO `gate:` KEY. One sample moving one channel on a recipe he has ratified.

script_authority: 'Node 002b-first-citizen, live script `002b-t0-c`,
  `approved_by: founder`, `approved_on: 2026-08-03` (read from
  genomes/sapling/nodes/002b-first-citizen/leaves/002b-t0-c.yaml:17-18, the leaf the
  render gate itself reads). His `approved_quote` verbatim: "approve 002b except.. we
  need to make SURE the fig growing on the sapling is mentioned, last time we generated
  002b it just.. appeared. also, the sapling was able to flail a leaf in episode 1? why
  couldnt it do that in 002b?" Both `approval_conditions` are about beat 01''s fig and
  beat 16''s leaf; neither touches beat 13.'

consumer: >-
  Roman's next look at beat 13, as a PAIR against the `out-b13-idfix` frames - same four seeds, one channel apart. It ranks nothing and picks nothing; the taste call stays R4's.

success: >-
  Four PNGs at C:\banyan-farm\wave-goblin-prep\out-b13-idfix-r2\13-the-shade-wave1-s0..3.png with their
  .yaml provenance sidecars written at render time (7.2), all eight declared as
  artifacts so a missing one fails rc=92 instead of passing quietly. Whether the
  frames are good is R4's call, not this job's and not a metric's.

why: >-
  One variable against `ep2-b13-idfix-0812`: `no scars, no stitched skin` in the SDXL negative, the exact two terms `ep2-b02-idfix-r2-0812` proved on this checkpoint tonight. The positive is byte-identical and seeds, steps (40), guidance (7.5), size and harness are unchanged.

goblin_def: green skin, bald head, patchwork cloak
goblin_def_source: >-
  The frozen identity, ledger `ep2-frozen-identities-0812`, passed byte-identical to the `_idfix` round this answers. NOT touched here - the fault is the model spreading `patchwork` onto skin, and the fix is in the beat's negative, not in the definition. That the definition is the underlying CAUSE is argued separately, from the beat-08 control, in review/ep2-picks/wave2-needs-founder.md.

est_minutes: 2
needs:
- cuda
- vram20
- farm-venv

env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True

# The box's wave-drafts.yaml was scp'd from this repo and `findstr`-verified to
# carry `authored_b13_idfix_r2` before this job was queued, and the recipe below was
# dry-run on the box's real CLIP tokenizer. THE CHECK IS THE KEY, NOT THE
# WHOLE-FILE SHA: sibling lanes append their own variants tonight and every append
# moves the file sha without touching this variant. The harness REFUSES rather than
# falling back when a key is missing, so a stale copy fails for $0.
steps:
- name: measure
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '13'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b13_idfix_r2
  - --dry
- name: sample
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '13'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b13_idfix_r2
  - --out
  - C:\banyan-farm\wave-goblin-prep\out-b13-idfix-r2
  - --task
  - ep2-b13-idfix-r2-0812

# DURABILITY: every still this farm makes lands single-copy on a box nothing backs
# up. The courier runs `git add -A -- farm-out` and pushes on job_done, and
# farm-out is NOT gitignored, so publishing is a COPY and the runner stays
# byte-identical. allow_fail: a failed copy must never mark a good render failed.
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - |
    import glob, hashlib, os, shutil
    dst = "C:/banyan-farm/courier-box/farm-out/ep2-b13-idfix-r2"
    os.makedirs(dst, exist_ok=True)
    src = sorted(glob.glob("C:/banyan-farm/wave-goblin-prep/out-b13-idfix-r2/13-the-shade-wave1-s*.*"))
    lines = []
    for f in src:
        shutil.copy2(f, dst)
        # Hash the COPY, not the original: what this records has to be the bytes
        # that get committed, or the manifest attests to the wrong file.
        c = os.path.join(dst, os.path.basename(f))
        with open(c, "rb") as fh:
            h = hashlib.sha256(fh.read()).hexdigest()
        lines.append(h + "  " + os.path.basename(f))
        print("   ", os.path.basename(f), h[:16])
    # newline="\n" is load-bearing: Windows text mode rewrites \n to \r\n and every
    # filename then carries a trailing \r that shasum -c cannot open.
    with open(os.path.join(dst, "13-the-shade-idfix-r2.sha256"), "w", newline="\n") as fh:
        fh.write("\n".join(lines) + "\n")
    print("published", len(src), "file(s) + manifest ->", dst)
    # 4 stills + 4 sidecars. Fewer means the render wrote less than it promised.
    raise SystemExit(0 if len(src) == 8 else 1)

artifacts:
- C:\banyan-farm\wave-goblin-prep\out-b13-idfix-r2\13-the-shade-wave1-s0.png
- C:\banyan-farm\wave-goblin-prep\out-b13-idfix-r2\13-the-shade-wave1-s1.png
- C:\banyan-farm\wave-goblin-prep\out-b13-idfix-r2\13-the-shade-wave1-s2.png
- C:\banyan-farm\wave-goblin-prep\out-b13-idfix-r2\13-the-shade-wave1-s3.png
- C:\banyan-farm\wave-goblin-prep\out-b13-idfix-r2\13-the-shade-wave1-s0.yaml
- C:\banyan-farm\wave-goblin-prep\out-b13-idfix-r2\13-the-shade-wave1-s1.yaml
- C:\banyan-farm\wave-goblin-prep\out-b13-idfix-r2\13-the-shade-wave1-s2.yaml
- C:\banyan-farm\wave-goblin-prep\out-b13-idfix-r2\13-the-shade-wave1-s3.yaml
