# EPISODE 2, BEAT 07 CONFISCATE - r2 OF THE IDENTITY-FROZEN ROUND. One sample,
# four seeds, $0. 2026-08-12, the fixer lane.
#
# THE FIRST FAULT HERE IS A RECIPE BUG, NOT A MODEL FAILURE.
#
# THE SETTING IS FINE, AND THAT IS EVIDENCE WORTH RECORDING. Open sunny field
# 4/4, bright 4/4, no armor 4/4, bald 4/4. Beat 07 NAMES its setting ("morning
# field") and keeps it in every frame - the beat-06 diagnosis confirmed from the
# other direction on the same night, on the same checkpoint. Nothing about the
# setting is touched here.
#
# 1. THE RECIPE CONTRADICTS ITSELF, SO THE PARTNER COULD NEVER ARRIVE. The
#    positive asks for "his partner's shoulder just visible at frame edge"; the
#    negative sends `people in background`. The ban negates the thing the prompt
#    asserts, and the partner is absent in 4/4. This is the exact wording
#    discipline the beat-02 r2 got right - name SKIN, never the bare word
#    `stitches`, so the patchwork cloak survives - violated in the other
#    direction. `no people in background` RETIRES. `no crowd` STAYS, so the drift
#    the ban was actually reaching for keeps a guard on it.
# 2. HE POINTS AT THE CAMERA, NOT OFF-FRAME, 4/4 - s2 with a closed fist rather
#    than a finger. The beat needs him pointing AWAY at the goblin off-screen;
#    pointing down the lens is a different shot. One fault, so one channel takes
#    both halves: `pointing away off-frame to his left` in the positive and
#    `no pointing at viewer` in the negative.
# 3. THE PLAIN BROWN TUNIC IS NOT BROWN - off-white in s0 and s1, washed tan in
#    s2 and s3, against a positive that says "plain brown tunic" in those words.
#    `no white shirt` cannot negate the positive, because the positive asserts
#    BROWN. Paid for out of the retired contradiction.
#
# NOT FIXED HERE, ON PURPOSE, FOR THE THIRD BEAT RUNNING: the guard is a hard
# muscular adult man rather than the "silly harmless bureaucrat" he ratified.
# Beats 05, 06 and 07 all show it, and beat 05 SENDS the bureaucrat words and
# still drew a muscular man, so the words are known not to bind on their own.
# That makes it a wave-level note for the founder, not something a single round
# should quietly chase while claiming to move one channel.
#
# BOX DRY-RUN, THE SHIPPED RECIPE, on the box's real CLIP tokenizer:
#   positive 75/77   STYLE ANCHOR PRESENT: True   count tag '1boy'
#   'text' negated: True   negative recipe 74 -> sent 74 (budget 77)
#   `pointing at viewer` and `white shirt` both sent; `crowd`, `armor`, `helmet`
#   and `knight` intact; the only casualty is `realistic skin texture`.
#
# SEPARATE OUT DIRECTORY. Writes to `out-b07-idfix-r2`; overwrites no evidence.

id: ep2-b07-idfix-r2-0812
task: ep2-b07-idfix-r2-0812
node: 002b-first-citizen
beat: 7
runner: box
needs_gpu: true

priority: 44
max_attempts: 1
owner: overnight fixer lane, 2026-08-12 (reading the identity-frozen wave and firing one r2 per faulty beat)

# NO `gate:` KEY. One sample moving one channel on a recipe he has ratified.

script_authority: 'Node 002b-first-citizen, live script `002b-t0-c`,
  `approved_by: founder`, `approved_on: 2026-08-03` (read from
  genomes/sapling/nodes/002b-first-citizen/leaves/002b-t0-c.yaml:17-18, the leaf the
  render gate itself reads). His `approved_quote` verbatim: "approve 002b except.. we
  need to make SURE the fig growing on the sapling is mentioned, last time we generated
  002b it just.. appeared. also, the sapling was able to flail a leaf in episode 1? why
  couldnt it do that in 002b?" Both `approval_conditions` are about beat 01''s fig and
  beat 16''s leaf; neither touches beat 7.'

consumer: >-
  Roman's next look at beat 07, as a PAIR against the `out-b07-idfix` frames - same four seeds. It ranks nothing and picks nothing; the taste call stays R4's.

success: >-
  Four PNGs at C:\banyan-farm\wave-goblin-prep\out-b07-idfix-r2\07-confiscate-wave1-s0..3.png with their
  .yaml provenance sidecars written at render time (7.2), all eight declared as
  artifacts so a missing one fails rc=92 instead of passing quietly. Whether the
  frames are good is R4's call, not this job's and not a metric's.

why: >-
  Three edits against `ep2-b07-idfix-0812`, each with its own separately visible predicate: a partner at frame edge (previously banned by the recipe itself), an arm pointing away rather than at the lens, and a brown tunic. Seeds, steps (40), guidance (7.5), size, harness and the setting are unchanged.

goblin_def: green skin, bald head, patchwork cloak
goblin_def_source: >-
  Passed byte-identical to the `_idfix` round this answers. NO GOBLIN APPEARS IN BEAT 07 - the positive names only the guard and his partner - so the definition is inert here and is passed unchanged purely so the two rounds differ where this job says they differ.

est_minutes: 2
needs:
- cuda
- vram20
- farm-venv

env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True

# The box's wave-drafts.yaml was scp'd from this repo and `findstr`-verified to
# carry `authored_b07_idfix_r2` before this job was queued, and the recipe below was
# dry-run on the box's real CLIP tokenizer. THE CHECK IS THE KEY, NOT THE
# WHOLE-FILE SHA: sibling lanes append their own variants tonight and every append
# moves the file sha without touching this variant. The harness REFUSES rather than
# falling back when a key is missing, so a stale copy fails for $0.
steps:
- name: measure
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '7'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b07_idfix_r2
  - --dry
- name: sample
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '7'
  - --goblin-def
  - green skin, bald head, patchwork cloak
  - --variant
  - authored_b07_idfix_r2
  - --out
  - C:\banyan-farm\wave-goblin-prep\out-b07-idfix-r2
  - --task
  - ep2-b07-idfix-r2-0812

# DURABILITY: every still this farm makes lands single-copy on a box nothing backs
# up. The courier runs `git add -A -- farm-out` and pushes on job_done, and
# farm-out is NOT gitignored, so publishing is a COPY and the runner stays
# byte-identical. allow_fail: a failed copy must never mark a good render failed.
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - |
    import glob, hashlib, os, shutil
    dst = "C:/banyan-farm/courier-box/farm-out/ep2-b07-idfix-r2"
    os.makedirs(dst, exist_ok=True)
    src = sorted(glob.glob("C:/banyan-farm/wave-goblin-prep/out-b07-idfix-r2/07-confiscate-wave1-s*.*"))
    lines = []
    for f in src:
        shutil.copy2(f, dst)
        # Hash the COPY, not the original: what this records has to be the bytes
        # that get committed, or the manifest attests to the wrong file.
        c = os.path.join(dst, os.path.basename(f))
        with open(c, "rb") as fh:
            h = hashlib.sha256(fh.read()).hexdigest()
        lines.append(h + "  " + os.path.basename(f))
        print("   ", os.path.basename(f), h[:16])
    # newline="\n" is load-bearing: Windows text mode rewrites \n to \r\n and every
    # filename then carries a trailing \r that shasum -c cannot open.
    with open(os.path.join(dst, "07-confiscate-idfix-r2.sha256"), "w", newline="\n") as fh:
        fh.write("\n".join(lines) + "\n")
    print("published", len(src), "file(s) + manifest ->", dst)
    # 4 stills + 4 sidecars. Fewer means the render wrote less than it promised.
    raise SystemExit(0 if len(src) == 8 else 1)

artifacts:
- C:\banyan-farm\wave-goblin-prep\out-b07-idfix-r2\07-confiscate-wave1-s0.png
- C:\banyan-farm\wave-goblin-prep\out-b07-idfix-r2\07-confiscate-wave1-s1.png
- C:\banyan-farm\wave-goblin-prep\out-b07-idfix-r2\07-confiscate-wave1-s2.png
- C:\banyan-farm\wave-goblin-prep\out-b07-idfix-r2\07-confiscate-wave1-s3.png
- C:\banyan-farm\wave-goblin-prep\out-b07-idfix-r2\07-confiscate-wave1-s0.yaml
- C:\banyan-farm\wave-goblin-prep\out-b07-idfix-r2\07-confiscate-wave1-s1.yaml
- C:\banyan-farm\wave-goblin-prep\out-b07-idfix-r2\07-confiscate-wave1-s2.yaml
- C:\banyan-farm\wave-goblin-prep\out-b07-idfix-r2\07-confiscate-wave1-s3.yaml
