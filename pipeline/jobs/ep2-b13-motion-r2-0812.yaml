# r2 2026-08-12. r1 acted better than anything else in the wave and sat on a tripod.
#
# r1's verdict: the blocking is RIGHT -- he sinks, draws his knees up, wraps his arms round
# them and tucks his head down, which is "folds into the shade, knees up around his ears"
# almost word for word. He does it perched on a photographic tripod, because this recipe's
# style header named one and the model drew it. Three of three clips carrying that phrase
# grew a tripod; the one without it did not.
#
# ONE VARIABLE: the prop language. Same init, same sha, same seed, same 97 frames, same
# action text -- the acting already works and must not be disturbed. If the tripod goes and
# the fold stays, beat 13 is the first beat in this episode whose motion is done.
id: ep2-b13-motion-r2-0812
task: ep2-b13-motion-r2-0812
node: 002b-first-citizen
beat: 13
runner: box
priority: 19
needs_gpu: true
max_attempts: 1
sample: true
owner: ep2 motion lane, 2026-08-12
consumer: 'review/ep2-picks/motion-0812.html — beat 13 of the goblin motion wave, fired on the passed b15 action probe. Screened as a set, one pass.'
success: 'A 704x1280 mp4, 97 frames, in which the goblin performs beat 13''s own action — folding, sinking, settling — while staying the same goblin, staying put in frame, and with the camera locked. The failure worth reporting is a portrait that only breathes: that would mean b15 was a lucky beat rather than a working recipe.'
why: The b15 probe passed on every predicate, which is the condition this wave was authorised on. Beat length 4s.
est_minutes: 6
needs:
- cuda
- vram20
- video-venv
env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
payload:
  C:\banyan-farm\ep2-b13-motion-r2-0812\cover_crop.py: "#!/usr/bin/env python3\n\"\"\"Cover-crop an asserted source image to an exact WxH. No model, no network.\"\"\"\nimport argparse, hashlib, os, sys\nfrom PIL import Image\n\nap = argparse.ArgumentParser()\nap.add_argument(\"--src\", required=True)\nap.add_argument(\"--sha256\", default=\"\", help=\"asserted before anything is written\")\nap.add_argument(\"--out\", required=True)\nap.add_argument(\"--size\", default=\"704x1280\")\na = ap.parse_args()\n\nif not os.path.isfile(a.src):\n    sys.exit(\"!! src not found: %s\" % a.src)\nh = hashlib.sha256()\nwith open(a.src, \"rb\") as fh:\n    for chunk in iter(lambda: fh.read(1 << 20), b\"\"):\n        h.update(chunk)\nhave = h.hexdigest()\nif a.sha256 and have != a.sha256:\n    sys.exit(\"!! SRC SHA MISMATCH -- refusing.\\n   want %s\\n   have %s\" % (a.sha256, have))\n\nW, H = (int(v) for v in a.size.split(\"x\"))\nim = Image.open(a.src).convert(\"RGB\")\nsw, sh = im.size\nscale = max(W / float(sw), H / float(sh))\nnw, nh = int(round(sw * scale)), int(round(sh * scale))\nim = im.resize((nw, nh), Image.LANCZOS)\nleft, top = (nw - W) // 2, (nh - H) // 2\nim = im.crop((left, top, left + W, top + H))\nos.makedirs(os.path.dirname(os.path.abspath(a.out)) or \".\", exist_ok=True)\nim.save(a.out)\nprint(\"src %dx%d sha %s\" % (sw, sh, have), flush=True)\nprint(\"cover-crop scale=%.4f -> %dx%d, centre box (%d,%d)\" % (scale, nw, nh, left, top), flush=True)\nprint(\"WROTE %s\" % a.out, flush=True)"
  C:\banyan-farm\ep2-b13-motion-r2-0812\b13-motion-prompt.txt: '2D anime, hand-drawn cel animation, static locked framing, the frame never moves, flat cel shading, clean ink linework, anime key art. Subject already in frame: a small goblin standing in a field under morning light — pale yellow-green skin, a bald domed head, long pointed ears, red eyes, a patched hooded coat. HE SINKS DOWN AND FOLDS UP SMALL: he slides lower, drawing his knees up toward his chest until they are almost at his ears, wrapping his arms around them and letting his head settle down between his shoulders. He is settling into a smaller shape in the same spot. He stays in the same place in frame and the camera does not move. Soft morning light, cinematic lighting, detailed, newest, masterpiece, best quality, very aesthetic.'
  C:\banyan-farm\ep2-b13-motion-r2-0812\b13-negative.txt: camera pan, camera tilt, panning, tilting, camera movement, dolly, zoom, push in, pull back, tripod, camera, camera equipment, film equipment, stool, face morphing, distorted face, warped face, changing facial features, extra eyes, extra ears, hair, walking out of frame, leaving the frame, drifting sideways, moving to a different spot, second character, duplicate character, scene change, shot change, new camera angle, different location, photorealistic, 3D render, CGI, live action, motion blur, text
  C:\banyan-farm\ep2-b13-motion-r2-0812\b13-jobs-encode.json: "[{\"beat\": 13,\n  \"embeds\": \"C:\\\\banyan-farm\\\\ep2-b13-motion-r2-0812\\\\b13-embeds.pt\",\n  \"prompt_file\": \"C:\\\\banyan-farm\\\\ep2-b13-motion-r2-0812\\\\b13-motion-prompt.txt\",\n  \"negative_file\": \"C:\\\\banyan-farm\\\\ep2-b13-motion-r2-0812\\\\b13-negative.txt\"}]"
  C:\banyan-farm\ep2-b13-motion-r2-0812\b13-jobs-render.json: "[{\"beat\": 13,\n  \"seed\": 20260806,\n  \"embeds\": \"C:\\\\banyan-farm\\\\ep2-b13-motion-r2-0812\\\\b13-embeds.pt\",\n  \"init\": \"C:\\\\banyan-farm\\\\ep2-b13-motion-r2-0812\\\\b13-init-704x1280.png\",\n  \"out\": \"C:\\\\banyan-farm\\\\ep2-b13-motion-r2-0812\\\\13-the-shade-LTX-motion-r2-0812.mp4\"}]"
steps:
- name: crop
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\ep2-b13-motion-r2-0812\cover_crop.py
  - --src
  - C:\banyan-farm\goblin-ipa-0812\out-b13-frozen\13-the-shade-ipa-r0-w015-s1.png
  - --sha256
  - 5065fb22b54d845ba215bca0bc99d7e82d550231e4778eadd855433d080bd8b6
  - --out
  - C:\banyan-farm\ep2-b13-motion-r2-0812\b13-init-704x1280.png
  - --size
  - 704x1280
- name: encode
  argv:
  - C:\banyan-video\venv\Scripts\python.exe
  - C:\banyan-farm\banyan-city\pipeline\ltx_i2v.py
  - --stage
  - encode
  - --jobs
  - C:\banyan-farm\ep2-b13-motion-r2-0812\b13-jobs-encode.json
- name: render
  argv:
  - C:\banyan-video\venv\Scripts\python.exe
  - C:\banyan-farm\banyan-city\pipeline\ltx_i2v.py
  - --stage
  - render
  - --jobs
  - C:\banyan-farm\ep2-b13-motion-r2-0812\b13-jobs-render.json
  - --size
  - 704x1280
  - --frames
  - '97'
  - --fps
  - '24'
  - --guidance
  - '2.0'
  - --distilled-sigmas
  - --two-stage
  - --image-crf
  - '33'
  - --offload
  - sequential
  - --mode
  - production
  - --task
  - ep2-b13-motion-r2-0812
  - --worker
  - rtx5090
  - --bench-jsonl
  - C:\banyan-farm\ep2-b13-motion-r2-0812\bench-b13-motion-r2.jsonl
  - --bench-label
  - ep2-b13-motion-r2-0812
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - |
    import glob, hashlib, os, shutil
    dst = "C:/banyan-farm/courier-box/farm-out/ep2-b13-motion"
    os.makedirs(dst, exist_ok=True)
    src = sorted(glob.glob("C:/banyan-farm/ep2-b13-motion-r2-0812/13-the-shade-LTX-motion-r2-0812.mp4*")
                 + glob.glob("C:/banyan-farm/ep2-b13-motion-r2-0812/b13-init-704x1280.png")
                 + glob.glob("C:/banyan-farm/ep2-b13-motion-r2-0812/bench-b13-motion-r2.jsonl"))
    lines = []
    for f in src:
        shutil.copy2(f, dst)
        c = os.path.join(dst, os.path.basename(f))
        with open(c, "rb") as fh:
            h = hashlib.sha256(fh.read()).hexdigest()
        lines.append(h + "  " + os.path.basename(f))
    with open(os.path.join(dst, "ep2-b13-motion-r2-0812.sha256"), "w", newline="\n") as fh:
        fh.write("\n".join(lines) + "\n")
    print("published", len(src), "file(s) + manifest ->", dst)
    raise SystemExit(0 if len(src) >= 2 else 1)
artifacts:
- C:\banyan-farm\ep2-b13-motion-r2-0812\13-the-shade-LTX-motion-r2-0812.mp4
- C:\banyan-farm\ep2-b13-motion-r2-0812\b13-init-704x1280.png
