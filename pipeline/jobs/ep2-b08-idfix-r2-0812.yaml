# EPISODE 2, BEAT 08 INSIDE HIM - r2 OF THE IDENTITY-FROZEN ROUND. One sample,
# four seeds, $0. 2026-08-12, the fixer lane.
#
# THE CAMERA, AND ONLY THE CAMERA, BECAUSE THIS BEAT HAS NO BUDGET FOR ANYTHING
# ELSE: 76/77 positive and 76/77 negative, the tightest pair in the wave, on the
# one beat that has already overrun its budget once (commit 6d177f4).
#
# WHY THE CAMERA AND NOT THE LOUDER FAULTS. Both of this beat's worst frames are
# the OVERHEAD ones. s0 shoots down on the pair and the guard reaches for the
# goblin's face instead of his belly. s1 shoots down and draws the goblin as a
# waist-high GREEN EGG with a face on it, flanked by two bald humans - the worst
# single frame in the wave. "The belly of a small goblin boy" read from directly
# above is a plausible route from a belly to a sphere. One change that plausibly
# reaches both bad frames beats three changes that each reach one, when the lane
# has one r2 and the beat has one token of headroom.
#
# RECORDED AND DELIBERATELY LEFT UNFIXED: the guard's tunic is near-black rather
# than "plain brown" (3/4), and NOBODY POINTS AT A BELLY in any frame (0/4),
# which is the beat's joke. Both are written up in wave2-needs-founder.md.
# Claiming to fix them here would mean pretending there were tokens to do it.
#
# THIS BEAT'S DEFINITION IS THE WAVE'S CONTROL AND IS NOT TOUCHED. Beat 08 sends
# `green skin, bald head` with NO `patchwork cloak`, and it is the ONLY goblin
# beat whose skulls come back clean - green, unbroken, no cream dome, no sutures,
# 4/4 - while beats 02, 03 and 04, which carry `patchwork cloak` in the
# DEFINITION, show the sewn skull at 4/4, 3/4 and 4/4. Nobody designed that
# comparison; the wave happens to contain it, and it is the evidence pointing the
# scalp fault at the definition rather than at any beat. It only stays evidence if
# this beat keeps sending exactly what it sent, so `--goblin-def` here is
# `green skin, bald head` and nothing else.
#
# PAID FOR: `no building` and `no interior` retire (every frame is outdoors 4/4
# and `no house` stays). In the positive, `Deadpan medium two-shot` becomes
# `Eye level two-shot` - a TOKEN-NEUTRAL swap, two words for two.
#
# TWO RECIPES WERE MEASURED AND REJECTED FIRST, $0 EACH, AND THE REASON MATTERS.
# `Deadpan eye-level two-shot` FAILED the gate: positive 50/77, STYLE ANCHOR
# MISSING, `POSITIVE DROPPED: ... very aesthetic`. Un-hyphenating to `Deadpan eye
# level two-shot` failed IDENTICALLY. A control dry-run of the untouched baseline
# established that this beat sits at 76/77 with one token spare, so a positive
# landing on 77 does not squeak in - the compressor drops whole trailing segments
# and takes the entire style tail with them. A hyphenated `eye-level` costs THREE
# CLIP tokens, not one, and this beat's usable ceiling is 76, not 77.
#
# BOX DRY-RUN, THE SHIPPED RECIPE:
#   positive 75/77   STYLE ANCHOR PRESENT: True   count tag '2boys'
#   'text' negated: True   negative recipe 74 -> sent 74 (budget 77)
#   `high angle` and `from above` both sent; `female goblin` and `elf` intact.
#
# SEPARATE OUT DIRECTORY. Writes to `out-b08-idfix-r2`; overwrites no evidence.

id: ep2-b08-idfix-r2-0812
task: ep2-b08-idfix-r2-0812
node: 002b-first-citizen
beat: 8
runner: box
needs_gpu: true

priority: 44
max_attempts: 1
owner: overnight fixer lane, 2026-08-12 (reading the identity-frozen wave and firing one r2 per faulty beat)

# NO `gate:` KEY. One sample moving one channel on a recipe he has ratified.

script_authority: 'Node 002b-first-citizen, live script `002b-t0-c`,
  `approved_by: founder`, `approved_on: 2026-08-03` (read from
  genomes/sapling/nodes/002b-first-citizen/leaves/002b-t0-c.yaml:17-18, the leaf the
  render gate itself reads). His `approved_quote` verbatim: "approve 002b except.. we
  need to make SURE the fig growing on the sapling is mentioned, last time we generated
  002b it just.. appeared. also, the sapling was able to flail a leaf in episode 1? why
  couldnt it do that in 002b?" Both `approval_conditions` are about beat 01''s fig and
  beat 16''s leaf; neither touches beat 8.'

consumer: >-
  Roman's next look at beat 08, as a PAIR against the `out-b08-idfix` frames - same four seeds, one channel apart. It ranks nothing and picks nothing; the taste call stays R4's.

success: >-
  Four PNGs at C:\banyan-farm\wave-goblin-prep\out-b08-idfix-r2\08-inside-him-wave1-s0..3.png with their
  .yaml provenance sidecars written at render time (7.2), all eight declared as
  artifacts so a missing one fails rc=92 instead of passing quietly. Whether the
  frames are good is R4's call, not this job's and not a metric's.

why: >-
  One channel against `ep2-b08-idfix-0812`: the camera height, banned in the negative and asserted in the positive by a token-neutral swap. Seeds, steps (40), guidance (7.5), size, harness and the goblin definition are unchanged, so any difference in these frames is the framing.

goblin_def: green skin, bald head
goblin_def_source: >-
  THE WAVE CONTROL, and deliberately NOT the frozen triple. Beat 08 sends `green skin, bald head` with no `patchwork cloak`, exactly as the `_idfix` round did. It is the only goblin beat whose skulls come back clean, and that comparison is the evidence implicating `patchwork` in the identity slot (wave2-needs-founder.md). Changing it here would destroy the control.

est_minutes: 2
needs:
- cuda
- vram20
- farm-venv

env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True

# The box's wave-drafts.yaml was scp'd from this repo and `findstr`-verified to
# carry `authored_b08_idfix_r2` before this job was queued, and the recipe below was
# dry-run on the box's real CLIP tokenizer. THE CHECK IS THE KEY, NOT THE
# WHOLE-FILE SHA: sibling lanes append their own variants tonight and every append
# moves the file sha without touching this variant. The harness REFUSES rather than
# falling back when a key is missing, so a stale copy fails for $0.
steps:
- name: measure
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '8'
  - --goblin-def
  - green skin, bald head
  - --variant
  - authored_b08_idfix_r2
  - --dry
- name: sample
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\wave-goblin-prep\render_wave_sample.py
  - --harness
  - C:\banyan-farm\wave-goblin-prep
  - --root
  - C:\banyan-farm\wave-goblin-prep\repo
  - --beat
  - '8'
  - --goblin-def
  - green skin, bald head
  - --variant
  - authored_b08_idfix_r2
  - --out
  - C:\banyan-farm\wave-goblin-prep\out-b08-idfix-r2
  - --task
  - ep2-b08-idfix-r2-0812

# DURABILITY: every still this farm makes lands single-copy on a box nothing backs
# up. The courier runs `git add -A -- farm-out` and pushes on job_done, and
# farm-out is NOT gitignored, so publishing is a COPY and the runner stays
# byte-identical. allow_fail: a failed copy must never mark a good render failed.
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - |
    import glob, hashlib, os, shutil
    dst = "C:/banyan-farm/courier-box/farm-out/ep2-b08-idfix-r2"
    os.makedirs(dst, exist_ok=True)
    src = sorted(glob.glob("C:/banyan-farm/wave-goblin-prep/out-b08-idfix-r2/08-inside-him-wave1-s*.*"))
    lines = []
    for f in src:
        shutil.copy2(f, dst)
        # Hash the COPY, not the original: what this records has to be the bytes
        # that get committed, or the manifest attests to the wrong file.
        c = os.path.join(dst, os.path.basename(f))
        with open(c, "rb") as fh:
            h = hashlib.sha256(fh.read()).hexdigest()
        lines.append(h + "  " + os.path.basename(f))
        print("   ", os.path.basename(f), h[:16])
    # newline="\n" is load-bearing: Windows text mode rewrites \n to \r\n and every
    # filename then carries a trailing \r that shasum -c cannot open.
    with open(os.path.join(dst, "08-inside-him-idfix-r2.sha256"), "w", newline="\n") as fh:
        fh.write("\n".join(lines) + "\n")
    print("published", len(src), "file(s) + manifest ->", dst)
    # 4 stills + 4 sidecars. Fewer means the render wrote less than it promised.
    raise SystemExit(0 if len(src) == 8 else 1)

artifacts:
- C:\banyan-farm\wave-goblin-prep\out-b08-idfix-r2\08-inside-him-wave1-s0.png
- C:\banyan-farm\wave-goblin-prep\out-b08-idfix-r2\08-inside-him-wave1-s1.png
- C:\banyan-farm\wave-goblin-prep\out-b08-idfix-r2\08-inside-him-wave1-s2.png
- C:\banyan-farm\wave-goblin-prep\out-b08-idfix-r2\08-inside-him-wave1-s3.png
- C:\banyan-farm\wave-goblin-prep\out-b08-idfix-r2\08-inside-him-wave1-s0.yaml
- C:\banyan-farm\wave-goblin-prep\out-b08-idfix-r2\08-inside-him-wave1-s1.yaml
- C:\banyan-farm\wave-goblin-prep\out-b08-idfix-r2\08-inside-him-wave1-s2.yaml
- C:\banyan-farm\wave-goblin-prep\out-b08-idfix-r2\08-inside-him-wave1-s3.yaml
