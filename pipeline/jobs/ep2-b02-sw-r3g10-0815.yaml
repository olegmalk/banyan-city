# MOTION SWEEP, beat 02, 2026-08-15. ONE VARIABLE AT A TIME against
# ep2-b02-nw-0815 as R0 baseline (measured: mean|diff| after f1 = 4.78, but a
# period-2 duplicate cadence and a dead first ~1.2s and dead last ~0.6s).
#
# HELD CONSTANT across R0/R1/R2/R3: source plate + sha, cover-crop program,
# seed 20260815, positive prompt, 704x1280, 97 frames, 24 fps, two-stage
# distilled sigmas [1.0 .99375 .9875 .98125 .975 .909375 .725 .421875] then
# [.909375 .725 .421875], sequential offload.
#
# THE ONE VARIABLE HERE:
#   guidance_scale 2.0 -> 1.0. CONFOUNDED BY DESIGN: diffusers sets do_classifier_free_guidance = guidance_scale > 1.0, so at 1.0 the uncond pass disappears and the negative stops being applied as a SIDE EFFECT. R2 isolates the negative's content; this run isolates CFG itself.
id: ep2-b02-sw-r3g10-0815
task: ep2-b02-sw-r3g10-0815
node: 002b-first-citizen
beat: 2
runner: box
priority: 18
needs_gpu: true
max_attempts: 1
sample: true
owner: motion sweep, 2026-08-15
consumer: The episode-2 render recipe decision. Four ready jobs are on HOLD-awaiting-motion-research and this sweep is what clears them.
success: 'A 704x1280 97-frame mp4 scored the same two ways as R0: per-frame |diff| series (not just the average -- LTX motion onset is late and a flatline-then-ramp hides in a mean) and a BLIND cold read. The finding worth reporting is either variable moving the onset earlier, killing the period-2 duplicate cadence, or holding motion past frame ~80.'
why: 'guidance_scale 2.0 -> 1.0. CONFOUNDED BY DESIGN: diffusers sets do_classifier_free_guidance = guidance_scale > 1.0, so at 1.0 the uncond pass disappears and the negative stops being applied as a SIDE EFFECT. R2 isolates the negative''s content; this run isolates CFG itself.'
est_minutes: 8
needs:
- cuda
- vram20
- video-venv
env:
  PYTHONUTF8: '1'
  PYTHONIOENCODING: utf-8
  PYTHONUNBUFFERED: '1'
  HF_HOME: C:\Users\artvn\.cache\huggingface
  HF_HUB_DISABLE_XET: '1'
  HF_HUB_DOWNLOAD_TIMEOUT: '60'
  HF_HUB_DISABLE_SYMLINKS_WARNING: '1'
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
payload:
  C:\banyan-farm\ep2-b02-sw-r3g10-0815\cover_crop.py: "#!/usr/bin/env python3\n\"\"\"Cover-crop an asserted source image to an exact WxH. No model, no network.\"\"\"\nimport argparse, hashlib, os, sys\nfrom PIL import Image\n\nap = argparse.ArgumentParser()\nap.add_argument(\"--src\", required=True)\nap.add_argument(\"--sha256\", default=\"\", help=\"asserted before anything is written\")\nap.add_argument(\"--out\", required=True)\nap.add_argument(\"--size\", default=\"704x1280\")\na = ap.parse_args()\n\nif not os.path.isfile(a.src):\n    sys.exit(\"!! src not found: %s\" % a.src)\nh = hashlib.sha256()\nwith open(a.src, \"rb\") as fh:\n    for chunk in iter(lambda: fh.read(1 << 20), b\"\"):\n        h.update(chunk)\nhave = h.hexdigest()\nif a.sha256 and have != a.sha256:\n    sys.exit(\"!! SRC SHA MISMATCH -- refusing.\\n   want %s\\n   have %s\" % (a.sha256, have))\n\nW, H = (int(v) for v in a.size.split(\"x\"))\nim = Image.open(a.src).convert(\"RGB\")\nsw, sh = im.size\nscale = max(W / float(sw), H / float(sh))\nnw, nh = int(round(sw * scale)), int(round(sh * scale))\nim = im.resize((nw, nh), Image.LANCZOS)\nleft, top = (nw - W) // 2, (nh - H) // 2\nim = im.crop((left, top, left + W, top + H))\nos.makedirs(os.path.dirname(os.path.abspath(a.out)) or \".\", exist_ok=True)\nim.save(a.out)\nprint(\"src %dx%d sha %s\" % (sw, sh, have), flush=True)\nprint(\"cover-crop scale=%.4f -> %dx%d, centre box (%d,%d)\" % (scale, nw, nh, left, top), flush=True)\nprint(\"WROTE %s\" % a.out, flush=True)"
  C:\banyan-farm\ep2-b02-sw-r3g10-0815\b02-prompt.txt: He drops low and presses himself flat to the ground. 2D anime, hand-drawn cel animation, flat cel shading, clean ink linework, anime key art, cinematic lighting, detailed, newest, masterpiece, best quality, very aesthetic.
  C:\banyan-farm\ep2-b02-sw-r3g10-0815\b02-negative.txt: camera pan, camera tilt, zoom, dolly, push in, pull back, tripod, cut to another shot, scene change, different location, split screen, still image, freeze frame
  C:\banyan-farm\ep2-b02-sw-r3g10-0815\b02-jobs-encode.json: "[{\"beat\": 2,\n  \"embeds\": \"C:\\\\banyan-farm\\\\ep2-b02-sw-r3g10-0815\\\\b02-embeds.pt\",\n  \"prompt_file\": \"C:\\\\banyan-farm\\\\ep2-b02-sw-r3g10-0815\\\\b02-prompt.txt\",\n  \"negative_file\": \"C:\\\\banyan-farm\\\\ep2-b02-sw-r3g10-0815\\\\b02-negative.txt\"}]"
  C:\banyan-farm\ep2-b02-sw-r3g10-0815\b02-jobs-render.json: "[{\"beat\": 2,\n  \"seed\": 20260815,\n  \"embeds\": \"C:\\\\banyan-farm\\\\ep2-b02-sw-r3g10-0815\\\\b02-embeds.pt\",\n  \"init\": \"C:\\\\banyan-farm\\\\ep2-b02-sw-r3g10-0815\\\\b02-init-704x1280.png\",\n  \"out\": \"C:\\\\banyan-farm\\\\ep2-b02-sw-r3g10-0815\\\\02-sweep-r3g10.mp4\"}]"
steps:
- name: crop
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - C:\banyan-farm\ep2-b02-sw-r3g10-0815\cover_crop.py
  - --src
  - C:\banyan-farm\courier-box\farm-out\ep2-b02-plate-0814\02-the-sprint-ipa-r0-w015-s0.png
  - --sha256
  - 8519ed910d70da5c9c66cf38a9d27cd4b7a62ae9d537b5ca4c1adbcff7c4e942
  - --out
  - C:\banyan-farm\ep2-b02-sw-r3g10-0815\b02-init-704x1280.png
  - --size
  - 704x1280
- name: encode
  argv:
  - C:\banyan-video\venv\Scripts\python.exe
  - C:\banyan-farm\banyan-city\pipeline\ltx_i2v.py
  - --stage
  - encode
  - --jobs
  - C:\banyan-farm\ep2-b02-sw-r3g10-0815\b02-jobs-encode.json
- name: render
  argv:
  - C:\banyan-video\venv\Scripts\python.exe
  - C:\banyan-farm\banyan-city\pipeline\ltx_i2v.py
  - --stage
  - render
  - --jobs
  - C:\banyan-farm\ep2-b02-sw-r3g10-0815\b02-jobs-render.json
  - --size
  - 704x1280
  - --frames
  - '97'
  - --fps
  - '24'
  - --guidance
  - '1.0'
  - --distilled-sigmas
  - --two-stage
  - --image-crf
  - '33'
  - --offload
  - sequential
  - --mode
  - production
  - --task
  - ep2-b02-sw-r3g10-0815
  - --worker
  - rtx5090
  - --bench-jsonl
  - C:\banyan-farm\ep2-b02-sw-r3g10-0815\bench-r3g10.jsonl
  - --bench-label
  - ep2-b02-sw-r3g10-0815
- name: publish
  allow_fail: true
  argv:
  - C:\banyan-farm\venv\Scripts\python.exe
  - -c
  - "import glob, hashlib, os, shutil\ndst = \"C:/banyan-farm/courier-box/farm-out/ep2-b02-sw-r3g10-0815\"\nos.makedirs(dst, exist_ok=True)\nsrc = sorted(glob.glob(\"C:/banyan-farm/ep2-b02-sw-r3g10-0815/02-sweep-r3g10.mp4*\")\n             + glob.glob(\"C:/banyan-farm/ep2-b02-sw-r3g10-0815/bench-*.jsonl\"))\nlines = []\nfor f in src:\n    shutil.copy2(f, dst)\n    c = os.path.join(dst, os.path.basename(f))\n    with open(c, \"rb\") as fh:\n        h = hashlib.sha256(fh.read()).hexdigest()\n    lines.append(h + \"  \" + os.path.basename(f))\nwith open(os.path.join(dst, \"ep2-b02-sw-r3g10-0815.sha256\"), \"w\", newline=\"\\n\") as fh:\n    fh.write(\"\\n\".join(lines) + \"\\n\")\nprint(\"published\", len(src), \"file(s) + manifest ->\", dst)\nraise SystemExit(0 if len(src) >= 2 else 1)\n"
artifacts:
- C:\banyan-farm\ep2-b02-sw-r3g10-0815\02-sweep-r3g10.mp4
- C:\banyan-farm\ep2-b02-sw-r3g10-0815\b02-init-704x1280.png
