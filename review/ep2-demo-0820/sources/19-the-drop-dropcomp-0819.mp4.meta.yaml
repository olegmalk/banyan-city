# Shot provenance (7.2) — copied forward, field for field, from the tool's own
# record at farm-out/ep2-b19-dropcomp-0819/19-the-drop-dropcomp-0819.yaml. That
# file is the authority; this one exists because render_t3.py resolves
# provenance from a `<clip>.mp4.meta.yaml` beside the clip in --clips, and a copy
# that leaves the sidecar behind publishes an empty platform, an empty model and
# a zero cost as if they were measured facts.
#
# DELIBERATE, and do not "tidy" it: no line in this file may contain the literal
# two-word sequence that render_t3.py's `held_still()` greps for (an unquoted
# nil model value). That grep is how the tool decides a beat is a held still and
# time-stretches it instead of looping it. This clip is 5.0000s against a
# 5.000s slot so neither branch can fire today, but the substring is a landmine
# for the next assembler and it is quoted out of existence on purpose.
platform: local-deterministic (pipeline/beat19_drop_animate.py, PIL/numpy, ffmpeg)
model: "none -- no model was loaded"
model_licence: 'n/a for this clip: nothing was sampled, so there is no model whose terms could apply to it. The PLATE it moves has its own record and its own licence, both below.'
# THE FIRST DRAFT OF THIS FILE PUT THE PLATE'S MODEL INSIDE `model:` AND
# build_site.publishable() REFUSED THE CLIP -- "not on the publish-safe
# allowlist (CreativeML Open RAIL++-M)". That refusal is worth writing down
# rather than quietly editing around, because the fix is NOT the interesting
# part and the hole it exposes is.
#
# The fix: `model:` is now what the tool's own authority record says it is, and
# the plate's provenance sits under `source_plate_provenance` -- which is the
# field layout that record already used. So this is a copy being faithful to
# its source, not a record being scrubbed to clear a gate. `model: <nil>` is a
# SENTINEL the gate honours by name ("declares no model rather than hiding
# one"), and it is the literally true statement about this clip: no sampler
# ran, there is no seed, no prompt and no negative.
#
# THE HOLE, NAMED BECAUSE NOBODY ELSE HAS: every one of the other seventeen
# motion clips in this cut is LTX i2v conditioned on an animagine plate, and
# every one of their sidecars names ONLY LTX -- whose licence IS on the
# allowlist. So they all pass a gate that refused this clip the moment its
# ancestry was written down, and they pass because that ancestry is not written
# down. Beat 19 is not being held to a looser standard here; it is being held
# to the same one, with the discrepancy published instead of hidden. The real
# question -- whether an animagine-derived plate may be redistributed at all,
# OpenRAIL++ being classified `unknown` and not `deny`, meaning nobody has read
# the terms -- is a human's to answer and is not answered by this assembly.
source_plate_provenance: >-
  animagine-xl-3.1 inpaint at strength 0.22 over pipeline/beat19_drop_composite.py's
  drawn sapling, seed 20260819, 40 steps, cfg 7.5, 832x1216, rtx5090, $0. Verbatim
  from the authority record. Licence: CreativeML Open RAIL++-M, which
  pipeline/licence_gate.py classifies `unknown` -- see the note above.
shot_beat: 19
size: 704x1280
seconds: 5.000
frames: 120
fps: 24
seed: 'none — no sampler ran. The plate''s seed is 20260819.'
task: ep2-b19-dropcomp-0819
cost_usd: 0
gpu_seconds: 0
wall_seconds: 44
prompt: ''
negative: ''
mode: composite
provisional: true
provisional_reason: >-
  PROVISIONAL in this clip''s own words: "NO. No plate_ack, no leaf, no lineage
  entry, nothing in any cut, and beat 19 stays a SLATE until the author says
  otherwise." It is in the 0819c BENCH cut as best-available footage on a
  steward ruling, and it is not a pick, not promoted and not canon.
source_plate: farm-out/ep2-b19-sapgloss-0819/b19-sapgloss-s20260819.png
source_plate_sha256: 5a62ff900df2e22c45df8d5b8c0a22c85f374a53cd7fa9b64483e7716d77c11c
bar: '8 of 8 clauses plus B5b. BAR: PASS — every clause. B6 (the founder''s disqualifying clause, zero fruit pixels in the body zone) passes with 109px of margin on every frame, structurally: the fig only ever travels right.'
demo_note: >-
  THE THIRD CLAUSE OF done_when IS NOT IN THIS CLIP AND IS NOT DISGUISED. The
  beat''s definition asks for the fall, the landing AND him noticing, in that
  order. Verbatim from the tool''s own honest_limits (a): "HE DOES NOT NOTICE.
  done_when asks for the fall, the landing AND him noticing, in that order; this
  clip delivers the first two exactly and the third not at all. A composite can
  move the fig and nothing else, and animating his head would destroy the one
  property that makes this clip trustworthy — that every pixel outside the fig''s
  corridor is bit-identical to the plate the author picked." Two further limits
  travel with it, also verbatim: (b) "THE LAST 2.792s IS A STILL FRAME. An honest
  hold, not a disguised one." (c) "THE EVENT IS SMALL: 48px of travel on a 33px
  fig in a 1280-tall frame."
palindrome_margin: >-
  0.0000s, by construction and not by luck. 120 frames / 24 = 5.0000s against a
  5.000s slot, so render_t3.py:616 needs dur > cdur + 0.05 and gets 0.000 — the
  reverse branch cannot be reached. Reversed, this beat would show a fig flying
  back up onto a stem; the earlier i2v take survived that on 0.09s of luck.
