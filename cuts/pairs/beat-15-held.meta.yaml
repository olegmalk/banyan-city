# Shot provenance (7.2) — written by hand 2026-08-07 when this clip was
# published to the review area. hold_still.py writes its sidecar beside
# the clip as <name>.mp4.meta.yaml, which is not the stem name the gate
# reads, and none was carried across with these four anyway.
platform: local-deterministic (pipeline/hold_still.py, ffmpeg)
model: none
model_licence: n/a — no video model ran, so no video model's licence
  attaches. The licence that DOES attach is the still's, recorded in
  genomes/sapling/nodes/001-capability-inventory/stills/README.md
  (cagliostrolab/animagine-xl-3.1, OpenRAIL++ — the open D15 debt).
shot_beat: 15
size: 704x1280
seconds: 2.583
source_still: 15-something-s-coming.png
sha256: a920879e10cd269caee2393e4944f432055dfcac1bda62a0e9dbff766d6316b0
cost_usd: 0
measured_median_motion: 1.67
measured_frozen_share_pct: 2
measurement: pipeline/collect_farm.py measure() — median frame-to-frame
  mean-abs-diff on 200px grayscale, and the share of frames under 0.2.
  These numbers CANNOT be compared against an animated take — a push-in
  moves every pixel, so a held still scores higher than real animation.
note: |-
  The approved still held and pushed in by code — the treatment this beat
  gets in ep1-v30-fixed.mp4. Nothing was generated; every pixel is the
  frame the founder approved.
prompt: |-
  (none — the approved still is held; nothing was generated)
negative: ''

# ---- CORRECTIONS — appended 2026-08-08 overnight. NOTHING ABOVE IS EDITED. ----
# WHICH PIXELS THIS CLIP HOLDS, as bytes. `15-something-s-coming.png` was
# re-promoted 2026-08-08 17:58 +0400 — the beat-15 redraw the founder passed that
# afternoon — and this clip was committed 2026-08-07 18:39. The frame held here
# is therefore the pre-promotion one, kept by R6 as
# `15-something-s-coming-REVOKED-underground.png`.
#
# Read out of the repository's history — `git rev-list -1 --before=<this clip's
# commit> HEAD -- <the still>`, then sha256 of that blob. CORROBORATED: its pair
# `beat-15-animated.meta.yaml` already recorded aa14d078… as its own
# `init_still_sha256`, measured a different way. Both halves of the comparison
# were drawn from the same frame, which is the whole point of a pair.
#
# THIS IS THE PAIR THE DEFECT WAS FOUND ON: with the name alone, the review page
# showed the NEW tall-sapling frame over both of these clips — footage of what
# came before it. The right-hand clip's hash already stopped that; this line
# stops it for the left-hand one, on the deploy as well as here.
source_still_sha256: aa14d078774b703cecc156f119fc0142e4a73ec350675790b6a094fe0d74d34a
