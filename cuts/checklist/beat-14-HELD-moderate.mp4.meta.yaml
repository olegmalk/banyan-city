# Shot provenance (7.2) — written by hold_still at build time
platform: local-deterministic (pipeline/hold_still.py, ffmpeg)
model: none
model_licence: n/a — inherits the still's licence, see stills/README.md
note: held still + code push-in, no video model ran
shot_beat: 14
size: 704x1280
seconds: 12.99
zoom: push-in 12.0% over 12.99s, 0.92%/s, linear, centred, monotonic (never reverses)
source_still: 14-worth-staying-in.png
cost_usd: 0
prompt: |-
  (none — the approved still is held; nothing was generated)
negative: ''

# ---- CORRECTIONS — appended 2026-08-09. NOTHING ABOVE IS EDITED. ----
# `14-worth-staying-in.png` was revoked on 2026-08-09 and R6 keeps its pixels in place as
# `14-worth-staying-in-REVOKED-illegible.png`: he refused that frame in v32 on 2026-08-07 — "beat 14 is.. i dont know what??
# what is it supposed to be?", and no
# replacement has been drawn, so NOTHING holds the old name now. This clip is
# still those exact pixels — what changed is the filename, not the picture.
#
# Read out of the repository's own history and not measured off this picture:
# `git rev-list -1 --before=<this clip's commit> HEAD -- <the still>`, then
# sha256 of that blob — the method beat-07-zoom-gentle records for the same
# class of rename. Recording it is what lets still_from_record find the bytes
# under their new name (rule 2); without it the review page withholds this
# clip's poster, because the filename it names is in no node's stills/.
source_still_sha256: 3c9b104225081f94549ed3f7adbfe2dd5ad88f88c45f9ecb1de3b1ec422e1b0b
