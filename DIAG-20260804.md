# DIAG 2026-08-04 — rtx5090 worker, task bench-animegen-b01-1785827400

**VERDICT: DEAD. The render subprocess died at 0xC0000005, and ~15 min later the
whole machine bluescreened and rebooted. Worker did not survive; nothing is
running now. I did NOT restart it — see "Why the worker is still down".**

> **RESOLVED 13:01 local.** `c004060` parked the animegen task, which cleared the
> only blocker. Worker restarted and is polling an empty queue; SSH is live and the
> Mac has reached it. See "Resolution" at the end. The diagnosis below is kept as
> written, for the record.

Written by Claude Code on the laptop. All times local (Arabian Standard Time,
UTC+4); heartbeat.txt timestamps are UTC.

## Current state (12:10 local / 08:10Z)

| Check | Result |
|---|---|
| `Get-Process python*` | **nothing** — no python of any kind |
| nvidia-smi util | **0%** |
| nvidia-smi VRAM | **0 MiB / 24463 MiB** |
| nvidia-smi processes | **none** |
| GPU temp / power | 37C, 24W / 145W — fully idle |
| Host RAM | 18.7 GB free of 31.4 GB; commit 14.1 / 89.4 GB |
| Disk C: | 612.9 GB free — not a factor |
| Machine uptime | booted **11:30:33** — i.e. it rebooted 40 min ago |

Not hung, not paging, not rendering. Dead, and the box under it restarted.

## What actually happened

```
11:12:11  worker pid 18328 takes the lock, STARTED task=bench-animegen-b01-1785827400
11:12:47  VIDEO_RENDERING beat=01 on animegen (single-process)
11:13:43  *** python.exe ACCESS_VIOLATION 0xc0000005 ***
          faulting module: C:\banyan-video\venv\Lib\site-packages\torch\lib\c10.dll
          fault offset 0x8ec04, faulting pid 0x4AC0 (the RENDER CHILD, not the worker)
11:17:50  VIDEO_ALIVE 5m elapsed,  0 clips
11:22:53  VIDEO_ALIVE 10m elapsed, 0 clips, last output 9m ago
11:27:56  VIDEO_ALIVE 15m elapsed, 0 clips, last output 14m ago   <- last heartbeat, pushed OK
~11:28-11:30  *** BUGCHECK 0x3B SYSTEM_SERVICE_EXCEPTION (0xc0000005) ***
          fault bucket AV_nllNetHub!unknown_function
          dump: C:\Windows\Minidump\080426-19203-01.dmp
11:30:33  machine back up. Worker gone, lock left behind.
```

Two separate deaths, ~15 minutes apart.

**Read the heartbeat correctly:** "VIDEO_ALIVE, last output 14m ago" did **not**
mean the render was alive. The child had already been dead for 14 minutes. The
parent was blocked on a corpse (Windows Error Reporting was collecting a minidump
+ ETL trace on it, which holds the process handle open). *"last output Nm ago"
climbing while elapsed climbs is the signature of a dead child, not a slow one* —
worth a watchdog rule on that ratio.

## Evidence quality notes

- `farm-out/worker-log.txt` is **0 bytes** — the exact failure the comment at
  farm_worker.py:175-181 warns about. Nothing in it.
- `farm-out/errors.txt` has **no entry for this task**. Last entry is
  2026-08-03T08:14:22Z. The BSOD killed the worker before it could write one.
- So **there is no Python traceback for this failure and there never will be** —
  0xC0000005 is a native access violation; the interpreter dies without unwinding.
  The Windows Application Error record above is the whole traceback.
- The 45-min stall watchdog never fired (start 11:12 + 45 = 11:57; box died 11:30).
- Last heartbeat commit `6015da3` **is on the remote** — `git ls-remote` matches.
  Nothing was lost in the crash. Working tree clean.

## This crash is a repeat, and it is the known one

`python.exe` + `0xc0000005` in torch's `c10.dll`, from Application Error log:

```
2026-08-04 11:13:43   c10.dll        <- this one
2026-08-03 12:14:01   c10.dll        <- errors.txt: exit 3221225477 = 0xC0000005
2026-08-03 11:31:55   c10.dll
2026-08-02 12:00:20   torch_cpu.dll
```

`pipeline/farm-queue.yaml:43-60` predicted this precisely and set the stop
condition: *"This task therefore re-runs the exact configuration that produced
0xC0000005... If it dies at 0xC0000005 again, the fix is one code change and not
a flag."* **That condition is now met.** The answer the sample was queued to get
has been obtained: the config still dies, still at 0xC0000005, still fast (~56s
from VIDEO_RENDERING to the fault).

## Why the worker is still down (deliberate, please read before restarting)

Restarting right now would do exactly one thing: re-run the task that just took
the machine down. Two reasons I stopped instead:

**1. The only outstanding queue item is the fatal one.** `faceneg-b01-1785819600`
already shows `DONE` in the heartbeat. `bench-animegen-b01-1785827400` is the sole
remaining task for this worker. There is no other work a restart would pick up.

**2. The retry guard cannot stop this loop — it is blind to BSODs.**
`finished_tasks()` counts `FAIL task=<id>` lines out of heartbeat.txt, and
`MAX_ATTEMPTS = 2`. Grepping heartbeat.txt for this task returns **only**:

```
07:12:18Z STARTED task=bench-animegen-b01-1785827400 beats=1 on cuda
```

No `FAIL` line — because writing one requires the worker to survive the failure,
and this failure kills the host. **Recorded attempt count for this task is 0.**
A restarted worker treats it as brand new. Every future BSOD erases the evidence
that would eventually stop the retries, so this is a crash loop the existing guard
is structurally unable to break. (The 2026-08-03 `anime-canary-b07` attempts *did*
accumulate FAIL lines precisely because those crashes killed only the child.)

This is a genuine gap in the guard, not just a fact about today: **any failure
mode that takes down the OS is invisible to the attempt counter.**

## The proposed fix may not be sufficient on this box — check this first

farm-queue.yaml:46-49 diagnoses the load path: `load_animegen` (wan_i2v.py:164-167)
loads both A14B transformers in bf16 *then* casts to fp8, peaking ~64 GiB host RAM,
with cpu-offload keeping **~38 GiB resident afterwards**. The proposed fix — load
`hi`, cast, then load `lo`, cast — takes the *peak* from ~64 to ~32 GiB.

> **CORRECTION (from the Mac, 13:07).** The reordered peak is **~48 GiB, not ~32** —
> the queue note's figure counted the bf16 half only. So the gap against 31.4 GB
> physical is worse than computed below, and the conclusion holds harder: AnimeGen
> is parked on this machine class regardless of load order. The fix landed on the
> Mac anyway for future >=64 GB hosts, gated to `--no-lora`, because an early fp8
> cast breaks peft LoRA injection (adapters build in the base dtype, giving a
> bf16 x fp8 matmul error).

Against this machine's actual numbers:

- **Physical RAM: 31.4 GB usable** (2 x 16 GB SK Hynix 5600).
- Pagefile: 58 GB, system-managed. Commit limit 89.4 GB.

So: peak ~64 GiB against 31.4 GB physical = ~33 GB of hard paging, which matches
the "HOST RAM" diagnosis. But note that **the fix only addresses the peak. The
~38 GiB steady-state offload residency is untouched by load order, and 38 GiB
still exceeds 31.4 GB of physical RAM.** The model would page continuously through
every offload step of all 25 steps, with an expert swap per step.

I have not proven that causes the access violation, but the honest read is that
the one-line load-order fix is necessary and probably **not sufficient** here, and
AnimeGen at 14B x 2 may simply not fit a 32 GB host regardless of load order.
Worth deciding before spending another sample on it. Cheapest disambiguation:
instrument peak working set during load, or test on a 64 GB host.

VAE tiling / offload status in the failing stage, since it was asked: `offload:
true` was set in the task and `enable_model_cpu_offload()` is on the AnimeGen path
(wan_i2v.py:187); wan_i2v.py:191 prints "AnimeGen: fp8 layerwise casting + cpu
offload + VAE tiling". **Neither was reachable as a cause this time** — the crash
was at 11:13:43, ~56s in, during model load, well before VAE encode. The prior
VAE `_encode` OOM (fixed in fb1e43e) is a *different* failure and did not recur.

## The bluescreen — flagging, not claiming

Bugcheck `0x3B` / `0xc0000005`, bucket **`AV_nllNetHub!unknown_function`**. That
names a networking component (this is an MSI box; `C:\MSI` present), **not** the
GPU or torch. It is 15 minutes after the python crash, so I am **not** asserting
the render caused it. Two readings, both open:

- Independent network-driver bug that happened to land mid-task; or
- Collateral from the machine thrashing ~33 GB into a pagefile.

Minidump is at `C:\Windows\Minidump\080426-19203-01.dmp` if someone wants to run
it through WinDbg. Kernel-Power 41 confirms the unclean shutdown. (Ignore the
EventLog 6008 "previous shutdown at 11:21:56" — that is the stale registry clock
value, and it contradicts a heartbeat that was pushed at 11:27:56.)

## Stale lock — will block the next start

```
C:\Users\artvn\AppData\Local\Temp\banyan-farm-rtx5090.lock
  pid 18328 started 2026-08-04T07:12:11Z
```

pid 18328 died with the box. The lock survived the reboot (it is outside the repo
by design, farm_worker.py:192-196). **The next start will refuse with "another
worker already holds this machine" until this is cleared** — that is the 2026-08-01
mutex working as intended, not a new fault. Clear it with `--force` or delete it.
I left it in place so nothing can auto-start into the crash loop before a human
has read this.

## To restart, once the queue question is settled

One console, from `C:\banyan-farm\banyan-city`:

```
C:\banyan-farm\venv\Scripts\python.exe pipeline\farm_worker.py --name rtx5090 --force
```

`--force` clears the stale lock. **Do not run this while
bench-animegen-b01-1785827400 is still the head task for rtx5090** unless the
intent is deliberately to re-run the fatal config. Unblock first, by either:

- **(a)** drop/park the animegen task in `pipeline/farm-queue.yaml` on `main` —
  then a restart is safe and the worker just idles on an empty queue; or
- **(b)** land the load-order change in `wan_i2v.py` as its own recipe change with
  its own sample and screening, per CLAUDE.md's one-sample rule and the explicit
  note at farm-queue.yaml:57-60 that it must not be smuggled into this task.

Both are the orchestrator's / founder's calls, not mine — I have not touched
`main`, the queue, or `wan_i2v.py`.

## SSH — LIVE as of 12:53 local. Mac can connect now.

Oleg approved each elevation via UAC; the privileged steps ran under his approval.

```
ssh artvn@192.168.3.157
```

| | |
|---|---|
| `sshd` | **Running**, StartType **Automatic** (survives reboot) |
| `ssh-agent` | Running, Automatic |
| Listening | `0.0.0.0:22` and `[::]:22`, pid 22304, `C:\Windows\System32\OpenSSH\sshd.exe` |
| Firewall | `OpenSSH-Server-In-TCP` — Enabled, Inbound, Allow, **Private profile only** |
| Wi-Fi IPv4 | **`192.168.3.157`** /24, DHCP |
| Network category | Private — matches the firewall rule, so the rule is in effect |
| Reachability | `Test-NetConnection 192.168.3.157 -Port 22` → **True** |
| Host keys | ecdsa, ed25519, rsa — generated on first service start |

DHCP, so the address can move on lease renewal — worth a reservation on the router
if this channel is going to be relied on.

### Install was not clean — read if it needs redoing

`Add-WindowsCapability` **never returns** on this box. Two attempts (12:21, 12:42),
both hung ~8 min then `CAddCapabilityCommandObject::InternalExecute(hr:0x800706be)`
= `RPC_S_CALL_FAILED` — the TrustedInstaller RPC channel dies. But **the install
work completes anyway**: binaries, service registration and the firewall rule all
landed. Only the cmdlet's return is lost, so any script calling it never reaches
its next line. That is why the service needed starting separately.

Likely root cause was the 11:30 BSOD interrupting servicing — `CBS RebootPending`
was True at 12:22 and `AppxDeploymentFailureBlue` (`0x80073CF3`) was logged 11:32.
Both have since cleared (`RebootPending` now False, no `pending.xml`).

**Loose end:** an elevated PowerShell (pid 10668) is still wedged inside that
cmdlet. Harmless but it holds a CBS session — close the window, or reboot when
convenient. `winget` v1.29.280 is present as a CBS-free fallback
(`Microsoft.OpenSSH.Beta`) if the capability ever needs reinstalling.

### Security state — two things to close

1. **Password authentication is ON.** `sshd_config:51` is `#PasswordAuthentication
   yes` — commented, so the built-in default (yes) applies. Anyone on this LAN who
   has or guesses the `artvn` password can get an admin shell. Mitigated only by
   the Private-profile rule (LAN-only, not internet-exposed).
2. **No key installed yet.** `C:\ProgramData\ssh\administrators_authorized_keys`
   does not exist. `artvn` is an administrator, and this box's `sshd_config`
   confirms the override — `Match Group administrators` sets `AuthorizedKeysFile`
   to `__PROGRAMDATA__/ssh/administrators_authorized_keys`, so a key in
   `~/.ssh/authorized_keys` **will be ignored**. That file also needs its ACL cut
   to Administrators + SYSTEM only, or sshd rejects it.

Recommended: put the Mac's public key in that file, confirm key auth works, then
set `PasswordAuthentication no` and restart sshd. I did not touch either — the key
has to come from the Mac, and I will not handle a password.

## What I did and did not do

Did: read-only diagnosis (process list, nvidia-smi, event logs, heartbeat,
errors.txt, git state, queue, worker source); wrote and pushed this file;
installed + started OpenSSH Server and verified it listens (each elevation
approved by Oleg at a UAC prompt), and created the Private-profile inbound rule
for TCP 22; installed the Mac's public key and cut its ACL.

Naming note for the record: the operator is **Oleg** (olegmlkvorg). Earlier
revisions of this file said "Alex" — that was a speech-to-text error in the
session opener, corrected here.

Did **not**: kill anything (nothing was alive to kill); restart the worker; clear
the lock; touch `main`, `farm-queue.yaml`, `wan_i2v.py`, the venvs, or
`C:\banyan-video`; install any SSH key; change password settings or handle any
credential; commit any secret (the deploy key lives at
`C:\banyan-farm\farm_deploy_key`, outside the repo, and was not read or copied).

**Next decision is the orchestrator's: (a) park the task, or (b) fix the load path
and re-sample. Until one of those, this worker stays down on purpose.**

## Resolution — 13:03 local

The orchestrator took option (a): `c004060` "park the animegen sample: it answered,
and the answer took the host down". Queue head for rtx5090 is now only
`faceneg-b01-1785819600`, already `DONE`, so a restart idles instead of re-running
the fatal config. That cleared the only reason to hold the worker down.

**Worker restarted 13:01:29** with the invocation above (`--force` cleared the stale
lock from dead pid 18328):

| Check | Result |
|---|---|
| Lock | retaken — `pid 17444 started 2026-08-04T09:01:30Z` |
| Polling | `.git/FETCH_HEAD` refreshed 13:02:43 — fetching `origin/main` each 60s cycle |
| GPU | 0% util, 0 MiB — correctly idle, no task claimed |
| Heartbeat | unchanged, and that is **expected**: with no work, `mark()` never fires, so an idle worker writes no heartbeat. Absence of heartbeat here is not absence of a worker. |
| Branch | still `farm-results-rtx5090` @ `c973e83`, this file intact |

Confirmed before restarting that the self-update path syncs pipeline files from
main *without* switching branches (a switch would delete `farm-out`, which is
tracked here and absent on main), and `Courier.mark()` re-checkouts this branch
before committing — so the worker cannot strand this report.

**SSH verified from the other side:** `MACBOOKPRO-C256` (192.168.3.127) reached
sshd at 12:59:35 and closed at preauth — consistent with an `ssh-keyscan`
fingerprint check. The LAN path and the Private-profile firewall rule both work
end to end. Host key fingerprints, read off the wire from the running sshd:

```
ED25519  SHA256:fI4tO7qvy/wdA4gj65QTtzA4JYpJktr+YClCJupTPgo
ECDSA    SHA256:XcUusFgtxjuppNmisCLTb8Xr3sfCMuaJCyqwRY9O/RA
RSA      SHA256:JZ3a7UvdanjRKRiCzHnKZyDL8V+P21V8wWP/9C1u1VA
```

### Still open

1. ~~No SSH key installed~~ **DONE 13:08.** The Mac's dedicated ed25519 key is
   installed at `C:\ProgramData\ssh\administrators_authorized_keys` — 107 bytes,
   one key line, no BOM, ACL broken from inheritance and granted to exactly
   `NT AUTHORITY\SYSTEM:(F)` and `BUILTIN\Administrators:(F)`. No sshd restart was
   needed; `authorized_keys` is read per connection.

   Installed fingerprint: `SHA256:LyL9it1hQEgkGB7Wn6MmqKm27FN8cghhhjZ2OB0CGCE`
   (ED25519, comment `mac-orchestrator->rtx5090`). Cross-check that on the Mac
   against `ssh-keygen -lf ~/.ssh/banyan-rtx5090.pub` — it confirms the key was not
   altered in transit through chat.

   **`PasswordAuthentication` is deliberately still on** (`sshd_config:51`, still
   commented). Per the Mac's own sequencing: it tests `ssh rtx5090`, confirms a
   successful key login, and only then do I set `PasswordAuthentication no` and
   restart sshd. Disabling it first risks locking both machines out.
2. **Wedged elevated PowerShell, pid 10668** — still inside the `Add-WindowsCapability`
   call that never returns. Harmless, holds a CBS session; closing the window or a
   reboot clears it.
3. **The BSOD is NORTON.** `nllNetHub.sys` is Norton's network filter driver —
   637 KB in `C:\Windows\System32\drivers`, and the giveaway is its sibling
   `nllVpnRdr.sys`, which registers with DisplayName **"Norton VPN Driver"**. Same
   `nll` prefix, same directory, same 2026-07-31 20:39 install batch; 17 `nll*`
   drivers are loaded and running. Registered AV is **Norton 360 for Gamers**
   (`C:\Program Files\Norton\Suite`), with services Norton Antivirus, Norton
   Firewall, Norton Tools, Norton VPN all Running/Automatic.

   So bugcheck `0x3B` / bucket `AV_nllNetHub!unknown_function` reads as an **access
   violation inside Norton's network filter driver** — which is what rebooted the
   host and killed the worker at 11:30. Windows' own fault attribution, so strong,
   though not proof: a bucket can blame the driver that touched corrupted memory
   rather than the one that corrupted it. `C:\Windows\Minidump\080426-19203-01.dmp`
   would settle it.

   Norton is also the best remaining candidate for the servicing failures — the
   `0x800706be` RPC break in `Add-WindowsCapability` (twice) and the
   `AppxDeploymentFailureBlue` at 11:32. Not established, but it fits.

   **Recommendation: remove Norton from this machine.** Killing the processes
   achieves nothing — it self-protects, the services restart, and the kernel driver
   stays loaded; only an uninstall unloads `nllNetHub.sys`. Windows Defender is
   already registered alongside it and takes over automatically, so the box is not
   left unprotected. This is a security-posture change and possibly a paid
   subscription, so it is Oleg's decision and his click — not something I will do
   on his behalf.

   Sequencing when it happens: uninstall needs a reboot, which stops the worker
   (restart with `--force`) and clears the wedged pid 10668. Norton Firewall is
   currently Running, so **re-verify port 22 is still reachable afterwards** — our
   rule lives in Windows Firewall and should govern, but the path changes owner.
   Worth adding Defender exclusions for `C:\banyan-video` and `C:\banyan-farm` too,
   so real-time scanning stops reading every multi-GB model load.
